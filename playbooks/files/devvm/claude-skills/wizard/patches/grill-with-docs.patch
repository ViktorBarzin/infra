--- a/SKILL.md	2026-08-30 17:30:00.552909804 +0000
+++ b/SKILL.md	2026-08-30 17:30:00.555909831 +0000
@@ -5,3 +5,11 @@
 ---
 
 Call the Skill tool twice, for "grilling" and "domain-modeling".
+
+When the plan/design reaches shared understanding, write it to its canonical
+markdown home (the owning repo's docs tree). Sketching the design as a
+```` ```mermaid ```` diagram (architecture / flow / sequence) is part of the
+synthesis, not decoration. If the design has a shape, the document shows it.
+Then finish with the `publish-page` skill: render + push it and hand the user
+the `pages.viktorbarzin.me` URL. The published page is the session's
+deliverable.
