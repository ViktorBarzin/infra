# fixer-drill target

This file exists so a drill can create a defect whose fix requires a
real commit. Nothing else reads it.

Infra issues are filed on Forgejo at `viktor/infra`, and the `broken` label dispatches the fixer.
