# Update Knowledge Base

Record new learnings in the repo's agent docs: `AGENTS.md` for a rule every
session needs, otherwise the matching `docs/agents/*.md` reference file (service
notes, monitoring, networking, known issues, and the rest listed in `AGENTS.md`).

Add or update information based on recent discoveries about:
- Service versions
- Infrastructure patterns
- Important configurations
- Useful commands
- Troubleshooting notes

Context to add: $ARGUMENTS
