# Terraform Plan

Run terraform plan to preview infrastructure changes via SSH.

```bash
ssh wizard@10.0.10.10 "cd /home/wizard/code/infra && terraform plan"
```

Summarize the planned changes, highlighting any resources being destroyed or recreated.
