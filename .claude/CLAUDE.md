# Claude Code — Project Configuration

> **Shared knowledge**: Read `AGENTS.md` at repo root for architecture, patterns, rules, and operations. This file adds Claude-specific features on top.

## Claude-Specific Resources
- **Skills**: `.claude/skills/` (7 active). Archived runbooks: `.claude/skills/archived/`
- **Agents**: All agents are global (`~/.claude/agents/`, shared via dotfiles). Install Viktor's dotfiles for the full set.
  - **Infra specialists**: cluster-health-checker, dba, home-automation-engineer, network-engineer, observability-engineer, platform-engineer, security-engineer, sre
  - **Incident pipeline**: post-mortem → sev-triage → sev-historian → sev-report-writer
  - **DevOps**: devops-engineer, deploy-app, review-loop
- **Reference**: `.claude/reference/` — patterns.md, service-catalog.md, proxmox-inventory.md, github-api.md, authentik-state.md
- **GitHub API**: `curl` with tokens from tfvars (`gh` CLI blocked by sandbox)

## Critical Rule: Terraform Only

**ALL infrastructure changes MUST go through Terraform/Terragrunt.** Never use `kubectl apply/edit/patch/set`, `helm install/upgrade`, or any manual cluster mutation as the final state.

- **No exceptions for "quick fixes"** — even one-line changes must be in `.tf` files and applied via `scripts/tg apply`
- **Apply locally OR let CI do it — but ALWAYS commit.** You don't have to wait for CI: with apply access you MAY run the apply yourself (`scripts/tg apply <stack>` / `homelab tf apply <stack>`), but **from the main checkout, never a worktree** (git-crypt'd `*.tfvars` come through as ciphertext under the worktree filter-bypass, so a worktree apply reads garbage). **Every applied change MUST be committed and pushed to `master` the same session** — the repo is the source of truth, so applied-but-uncommitted HCL is drift that the next CI apply / daily drift-detection will try to revert. Order either way: apply locally then commit + push (CI's changed-stack apply then no-ops), or commit + push and let CI apply. Never apply an uncommitted edit; never leave a committed change unapplied.
- **kubectl is for read-only operations and temporary debugging only** (get, describe, logs, exec, port-forward)
- **If a resource isn't in Terraform yet**, evaluate whether it can be added before making manual changes. If manual change is unavoidable (e.g., emergency), document it immediately and create the Terraform resource in the same session
- **kubectl scale/patch during migrations is acceptable** as a transient step, but the final state must be in Terraform and applied via `scripts/tg apply`
- **Helm values live in Terraform** (templatefile or inline) — never `helm upgrade` directly

Violations cause state drift, which causes future applies to break or silently revert changes.

## Critical Rule: the devvm goes through Ansible

The cluster's counterpart. **Machine-wide changes to the devvm (10.0.10.10) are
edits to `playbooks/devvm.yml`, not commands typed on the box** — packages,
`/usr/local/bin` binaries, systemd units, `/etc` config, resource limits, apt
sources.

```sh
ansible-playbook -i playbooks/inventory.ini playbooks/devvm.yml --check --diff  # always first
ansible-playbook -i playbooks/inventory.ini playbooks/devvm.yml                 # apply
```

A `--check` run against the live box should be a no-op; anything else is drift,
and it means either the box carries something undeclared or a committed change
has not been applied. Validated end to end on 2026-08-29 against a VM cloned
from Proxmox template 1000: playbook, then `apt install terminal-lobby`, then
all six services up and all eight verification probes passing.

Routed elsewhere by design: accounts, groups, clones and kubeconfigs come from
`roster.yaml` via `t3-provision-users.sh` (hourly; infra#88 ports that half),
and Terminal Lobby's own files ship in its Debian package.

## Instructions
- **"remember X"**: use the `homelab memory` CLI. The rule and the usage discipline live in `~/.claude/rules/10-homelab.md` + `20-execution.md` §M — not restated here, so the two cannot drift apart. Infra-specific addition: for knowledge that belongs to the repo rather than to a session, also update the relevant CLAUDE.md / `AGENTS.md`.
- **Apply**: Authenticate via `vault login -method=oidc`, then **`homelab tf plan|validate|apply <stack>`** (always the full form — bare `homelab tf` prints `unknown command: "tf"`, which reads as "the verb does not exist"). It wraps `scripts/tg`, which handles state decrypt/encrypt. **Do not run bare `terragrunt`/`terraform`.** On a Tier-1 stack a bare terragrunt dies in "Initializing the backend" with `pq: password authentication failed for user "<your-os-user>"`, because `PG_CONN_STR` is set only by `scripts/tg`. **That error is not a permission-tier limit** — reading it as one led to abandoning local verification for CI and hand-rolling the offline check with plain `terraform init`, 15 times over a month. `scripts/tg` adds `-auto-approve` for `--non-interactive` applies, and `-lock-timeout` (default `5m`, override via `TG_LOCK_TIMEOUT`) on every state-locking verb (`plan`/`apply`/`destroy`/`refresh`) so a contended state lock **waits** instead of failing instantly with `Error acquiring the state lock`.
- **New services need CI/CD** and **monitoring** (Prometheus/Uptime Kuma). CI = a GHA workflow on the repo's GitHub mirror (build + tests off-infra, ADR-0002); Woodpecker gets a deploy-only pipeline — never an in-cluster build.
- **New service**: Use `setup-project` skill for full workflow
- **Ingress**: `ingress_factory` module. **Auth** (`auth` string enum, default `"required"` — fail-closed). Pick by asking "what gates the app?":
  - `auth = "required"` — Authentik forward-auth gates every request. Use when the backend has **no built-in user auth** and Authentik is the only thing standing between strangers and the app (prowlarr, qbittorrent, netbox, phpipam, k8s-dashboard, any admin UI shipped without its own login).
  - `auth = "app"` — the backend handles its own user authentication (NextAuth, Django, OAuth, bearer-token API, etc.); Authentik would only break it. No middleware attached; the app's own login is the gate. Examples: immich, linkwarden, tandoor, freshrss, affine, actualbudget, audiobookshelf, novelapp. **Functionally identical to `"none"`** — the distinct name exists to record intent at the call site.
  - `auth = "public"` — Authentik anonymous binding via the dedicated `public` outpost (routes via `traefik-authentik-forward-auth-public` → `ak-outpost-public.authentik.svc:9000`). Strangers auto-bound to `guest`; logged-in users keep their identity in `X-authentik-username`. **Only works for top-level browser navigation** — CORS preflight rejects XHR/fetch and automation can't replay the cookie dance. Audit trail, not a gate.
  - `auth = "none"` — no Authentik, no own-auth claim. Use for Anubis-fronted content (Anubis is the gate), native-client APIs (Git, `/v2/`, WebDAV/CalDAV, CardDAV), webhook receivers, OAuth callbacks, and Authentik outposts themselves.
  - **Anti-exposure rule** (the reason `"app"` exists): only pick `"app"` or `"none"` AFTER you've verified the app has its own user auth (`"app"`) OR the endpoint is intentionally public (`"none"`). Default is `"required"` so accidental omission fails closed. **Convention**: when using `"app"` or `"none"`, add a comment line above the `auth = "..."` line stating what gates the app or why it's public. **Enforced by `scripts/tg`**: every `tg plan/apply/destroy/refresh` runs `scripts/check-ingress-auth-comments.py` against the current stack and aborts if any `auth = "app|none"` line lacks the preceding `# auth = "<tier>": ...` comment. Stack-scoped — untouched stacks aren't blocked until they're next edited.
  - **`allowed_groups`** (ADR-0023, infra#84) — for `auth = "required"` ONLY: the Authentik groups permitted to reach this host (default `["Home Server Admins"]`). Stamped as the `authentik.viktorbarzin.me/allowed-groups` annotation; the `authentik` stack reads every forward-auth ingress at apply time (`kubernetes_resources` data source) and renders the single **default-deny** `admin-services-restriction` expression policy — unlisted host / user-in-no-listed-group → **denied**. A **break-glass bypass** admits `authentik Admins` / `Home Server Admins` BEFORE the table, so admins can never lock themselves out regardless of table/generator state. Access is **group membership only** (the old chrome per-identity list is now the `Chrome Users` group; the `proxy_only` attribute path is gone). `scripts/tg` runs `check-allowed-groups.py` to reject a typo'd group name (a nonexistent group = silent lockout). Non-default rows: proxy→`Proxy Users`, k8s→`kubernetes-*`, t3→`T3 Users`, chrome→`Chrome Users`, postiz→`Postiz Users`; everything else defaults to admin-only. The catch-all provider/outpost/middleware are UNCHANGED — only the policy is generated. Adding a new gated app is denied until the `authentik` stack re-applies (fails safe). Design: `docs/plans/2026-07-26-authentik-forward-auth-group-authorization-design.md`.
  - **Catalog description (2026-08-15)** — every dashboard-visible ingress sets `"gethomepage.dev/description"` in `extra_annotations`. The same annotations feed `home.viktorbarzin.me` AND `homelab services`, which is how agents discover what we already self-host, so a row with only a hostname carries little. **Enforced by `scripts/tg`**: `check-ingress-descriptions.py` runs per-stack alongside the auth-comment and allowed-groups checks. Exempt: secondary/carve-out ingresses that opt out of the dashboard (`homepage_enabled = false` or an explicit `"gethomepage.dev/enabled" = "false"` override — one app, one tile); deferred where `extra_annotations` comes in as a variable (the factory pattern), since the caller supplies it. All 28 previously-blank rows were filled the same day.
  - **Anti-AI**: on by default when `auth = "none"` or `auth = "app"` (no Authentik to discourage bots); redundant on `"required"` and `"public"`.
  - **DNS**: `dns_type = "proxied"` (Cloudflare CDN — **creates NO record**: proxied hostnames ride the zone-wide `*` wildcard CNAME, ADR-0021; the value still drives the external-monitor annotation. Sole exception: the apex ingress in `stacks/blog` keeps an explicit `"@"` record, wildcards never cover the apex), `"non-proxied"` (direct A/AAAA to the public IP — explicit records shadow the wildcard), or `"internal"` (public A record carrying the INTERNAL Traefik LB IP `10.0.20.203` — resolvable everywhere, routable only from home LANs/WG sites/VPN; the record is reachability, NOT a gate — pair with `extra_middlewares = ["traefik-home-lans-only@kubernetescrd"]`, since direct-to-WAN-IP SNI requests still reach Traefik, and NEVER combine that allowlist with `"proxied"` — cloudflared pod source IPs sit inside 10/8 and would bypass it. For a browser-facing host, optionally prepend `"traefik-error-pages-403@kubernetescrd"` (opt-in, `stacks/traefik/.../error-pages.tf`) so a denied source IP gets the themed error page instead of Traefik's bare `Forbidden` text — it MUST come **before** the allowlist in the list (a middleware only intercepts what is downstream of it, and the allowlist short-circuits without calling next), and it is deliberately separate from the default-chain `error-pages` middleware (500-504) so 403s cluster-wide, incl. Authentik denials and API JSON 403s, stay untouched. Users: the immich-frame kiosks + family/hermes-agent/mladost3/torrserver, `docs/plans/2026-07-04-immich-frame-lan-only-design.md`). **`dns_type = "none"` on a `.me` host is NOT private** — since the `*` wildcard (2026-07-09) every recordless name resolves through the tunnel and reaches Traefik; internal-only ingresses MUST use `"internal"` or live on `.lan` (ADR-0021). Smoke-test target: `echo.viktorbarzin.me` (auth=public, header-reflecting backend).
- **Anubis PoW challenge** (`modules/kubernetes/anubis_instance/`): per-site reverse proxy that issues a 30-day JWT cookie after a tiny PoW solve. Use for **public, content-bearing sites without app-level auth** (blog, docs, wikis, static landing pages). Pattern: declare `module "anubis" { source = "../../modules/kubernetes/anubis_instance"; name = "X"; namespace = ...; target_url = "http://<backend>.<ns>.svc.cluster.local" }`, then in `ingress_factory` set `service_name = module.anubis.service_name`, `port = module.anubis.service_port`, `anti_ai_scraping = false`. **real-ip is AUTO-ATTACHED: `ingress_factory` detects an `anubis-*` backend by `service_name` and prepends `traefik-real-ip@kubernetescrd` automatically — no per-site `extra_middlewares` wiring, and you can't forget it (carve-out ingresses pointing at the bare backend correctly skip it).** The shared **real-ip** Traefik plugin (`stacks/traefik/modules/traefik/real-ip-plugin/`, peer-trust) rewrites X-Real-Ip **and `Cf-Connecting-Ip`** to the true client — trusting an incoming `Cf-Connecting-Ip` only from the cloudflared pod peer (`trustedProxyCIDRs = 10.10.0.0/16`) and otherwise using the unspoofable TCP peer — so the cookie is stable AND client-unspoofable on both proxied and non-proxied paths, and header-less requests never 500. **`Cf-Connecting-Ip` is normalised too (added 2026-08-15), for backends that key on it directly rather than on X-Real-Ip.** The origin is reachable without transiting Cloudflare (pfSense NATs WAN :443 straight to Traefik with no Cloudflare-source restriction), so an unfiltered `Cf-Connecting-Ip` let any such client choose the address the backend rate-limits and logs. **`stacks/matrix` is the case that matters today** — tuwunel runs with `ip_source=cf_connecting_ip`, so both its ingresses carry `traefik-real-ip@kubernetescrd` via `extra_middlewares`; keep that middleware attached for as long as that setting stands. Normalising the header also gave tuwunel a usable client IP on paths that never touch Cloudflare (LAN split-horizon DNS, WireGuard, tailnet, IPv6-direct), which previously made every DB-backed endpoint answer 500 while static ones like `/_matrix/client/versions` still returned 200. **This replaced the old `strip_x_real_ip` / `drop-x-real-ip` machinery, retired 2026-07-19** (the strip fixed the 2026-07-14 blank-page flap on proxied sites but 500'd header-less requests on non-proxied ones; post-mortem `docs/post-mortems/2026-07-14-anubis-x-real-ip-cookie-flap.md`). (The proxied-monitor synthetic XFF in `stacks/uptime-kuma` `ANUBIS_PROBE_HEADERS` is now redundant — real-ip stamps the peer so probes get 200 regardless — but harmless.) Shared ed25519 key in Vault `secret/viktor` -> `anubis_ed25519_key`; cookie scoped to `viktorbarzin.me` so one solve covers all Anubis-fronted subdomains. **Local networks bypass the challenge (2026-08-22)**: the module owns the `bots:` key and renders a `trusted-local-networks` ALLOW rule FIRST — private + CGNAT ranges (`var.trusted_local_cidrs`: 10/8, 172.16/12, 192.168/16, 100.64/10, fc00::/7, fe80::/10), matched against the real-ip-stamped X-Real-Ip so it cannot be forged from the internet. Our public egress IP is deliberately excluded, which is what makes the bypass unreachable from outside. **First position is load-bearing** — rules are first-match-wins and `_deny-pathological` imports `headless-browsers.yaml` (DENY on `HeadlessChrome`), so a rule placed later would still block local Playwright/agents. Consequence: local humans skip the interstitial AND local non-JS clients (curl, headless browsers, in-cluster probes) reach the real backend — which also means an in-cluster uptime monitor now probes the app, not the challenge page. Custom policies pass **`policy_rules_yaml`** (rules list only, no `bots:` key; renamed from `policy_yaml` so a stale caller fails at plan time). The same CIDR list is mirrored as the x402 gateway's `TRUSTED_CIDRS` env in `stacks/traefik` — the two live in different CI fan-out scopes, so each copy is applied where it is edited; **change both**. Accepted: local traffic also skips the AI/pathological denies, and a tailnet-enrolled device keeps the bypass until revoked in Headscale. Auth is untouched. Design: `docs/plans/2026-08-22-local-network-bot-wall-bypass-design.md`. **DO NOT put Anubis in front of Git/API/WebDAV/CLI endpoints** — clients without JS can't solve PoW. **Replicas auto-default to 2 when `shared_store_url` is set** (all instances pass `redis://redis-master.redis.svc.cluster.local:6379/<unique-db-index>` — challenge state in Redis, verified cross-pod 2026-07-14); without a store the module defaults to 1 replica because in-process challenge state makes pod-A-issued/pod-B-solved fail with `store: key not found` (HTTP 500). For path-level carve-outs (e.g. wrongmove has `/` behind Anubis but `/api` direct, blog has `/net-diag.sh` direct), declare a second `ingress_factory` with `ingress_path = ["/<path>"]` pointing at the bare backend service. Active on 7 stacks: blog (except `/net-diag.sh`), kms, f1 (f1-stream), cc (cyberchef), json (jsoncrack), home (homepage), wrongmove/real-estate-crawler (UI only). (www + travel retired.) (NOT privatebin — its XHR `POST /` breaks under the Anubis challenge, deliberately skipped per `stacks/privatebin/main.tf`; this list previously claimed pb wrongly, corrected 2026-07-12.) See `.claude/reference/patterns.md` "Anti-AI Scraping" for full layering.
- **Scale-to-zero (Sablier, ADR-0022)**: rarely-used HTTP services park at 0 replicas and wake on first request. Enrollment is THREE pieces in the owning stack: (1) `sablier = { group = "<name>" }` on the `ingress_factory` call (emits the per-ingress Middleware CR via `kubectl_manifest` — NOT `kubernetes_manifest`, whose type inference breaks on strategy shape changes; **default strategy = `dynamic`**: instant themed loading page, ghost theme, 5s poll, swaps to the app when ready — revised from blocking 2026-07-12 after live cold-hit UX; `strategy = "blocking"` per-service for API paths; sits AFTER auth so scanners never wake apps; `session_duration` default `3h`); (2) labels `"sablier.enable" = "true"` + `"sablier.group" = "<group>"` + `"sablier.ready-after" = "5s"` (settling delay covering Traefik endpoint propagation — prevents the 503-after-wake race) on the Deployment **metadata** (multi-deployment apps share one group and wake/park together — e.g. resume+printer); (3) `spec[0].replicas` added to `lifecycle.ignore_changes` with the `# SABLIER_MANAGED_REPLICAS` marker (grep it to find every enrolled site) so applies/drift never fight the scaler. Keep `replicas = 0` in HCL (parked default on recreate). **Eligibility checklist** (design doc `docs/plans/2026-07-12-scale-to-zero-sablier-design.md`): HTTP ingress through Traefik ONLY (TCP IngressRouteTCP services like whisper/piper CANNOT enroll — middleware never sees TCP; cluster-internal-only services like osm-routing have no ingress to hook), request-driven (no background jobs that sleeping kills), no WebSocket dependence (WS doesn't refresh sessions, upstream plugin #26), Uptime Kuma monitor must be status-only (probes get an immediate 200 via `ignoreUserAgent` WITHOUT waking the app — enrolled monitors are SHALLOW; the deep signal is the `SablierWakeFailed` alert; **internal Kuma monitors for enrolled apps MUST probe the ingress URL `https://<host>/`, never the ClusterIP Service** — direct-service probes ECONNREFUSED at 0 replicas; 2026-07-14 the cluster-health "internal down(13)" set was all this class, repointed to the ingress/novelapp pattern via the Kuma socket.io API, hackmd+send internal monitors paused alongside their hand-park, dupe monitor "CyberChef" id=319 deleted), GPU tenants only after the ADR-0016 budget retune and never together with the GPU demand-gate (`stacks/tts`). Server: `stacks/sablier` (hand-rolled, `sablier.sablier.svc:10000`, sessions in-memory; **a Sablier restart PARKS all sessionless enrolled workloads** — `auto-stop-on-startup`, deliberate clean-slate). Traefik side: vendored LOCAL plugin pinned at v1.3.0 in `stacks/traefik/modules/traefik/sablier-plugin/` (never fetched from plugins.traefik.io); **re-verify plugin load in traefik logs after ANY traefik upgrade — one broken plugin silently disables ALL plugins incl. api-token-middleware** (paperless-mcp's gate). Enrolled so far (pilot + wave 2, 2026-07-12): resume (group resume+printer), netbox, dashy, grampsweb, **postiz group** (postiz [helm — labels via `kubernetes_labels.postiz_sablier`] + temporal + elasticsearch), trek, health (both ingresses), drone-logbook (backup CronJob affinity relaxed to preferred — RWO-driven), privatebin (auth=none — the probe-exclusion validator), cyberchef, jsoncrack (their anubis-* gates stay running, never enrolled), stirling-pdf (slow JVM boot), city-guesser, networking-toolbox, excalidraw, tandoor, **k8s-dashboard group** (7 deployments: 5 Helm-owned via kubernetes_labels.dashboard_sablier + HCL token-injector & oauth2-proxy with sablier.active.replicas=2 — batch 4), pgadmin (dbaas tier-0 stack — batch 4), affine (STRATEGY=BLOCKING, not dynamic: its desktop/mobile sync clients hit the bearer-token API directly and would choke on a 200 HTML loading page; WS caveat accepted — batch 4). **learn was enrolled then DELIBERATELY UN-ENROLLED same day (Viktor, 2026-07-12): it serves plans.viktorbarzin.me (his on-the-go plan-review surface) + learn.viktorbarzin.me and must load instantly — always-on by choice despite passing the checklist; do not re-enroll.** **novelapp + annas-archive UN-ENROLLED 2026-07-14 (Viktor, cheap-to-run request-based apps):** novelapp is shared with Gheorghe — cold starts hurt him; right-sized 640→320Mi (live working set ~156Mi) so always-on pins little. annas-archive-stacks (ebooks ns, ingress `stacks`) has a server-side consumer: book-search's `STACKS_URL` ClusterIP queue/status calls can never trigger a sablier wake, so parking silently killed the Anna's Archive download pipeline (Book Search deep-health truthfully red since ~2026-06-27; monitor 286 restored to `/health/deep` after the un-park). Both always-on by choice — do not re-enroll.** **Vetted-INELIGIBLE (do not enroll without fixing the cited blocker):** annas-archive-stacks (book-search server-side ClusterIP consumer — navidrome class; un-enrolled 2026-07-14), wealthfolio (in-ns daily cluster-DNS sync + monthly pod-affinity job + hourly pg-sync sidecar), beadboard (was: beads-dispatcher polls it */2 via cluster DNS — **that blocker is GONE since 2026-08-16**, auto-dispatch is off, so beadboard is now worth re-evaluating for scale-to-zero), speedtest (in-app hourly scheduler IS the product), freedify ×2 (hand-rolled auth-free `/api/stream/` ingress bypasses sablier; AirPlay), navidrome (background Subsonic clients + homepage widget + freedify scan target), hackmd + send (WebSocket-core, plugin #26 — both HAND-PARKED at 0 replicas 2026-07-12, Viktor: unused; send's share links dead while parked, its external monitor disabled), whisper/piper (TCP), osm-routing (no ingress), GPU apps (ADR-0016 retune gate), chatterbox-tts (**scheduling fully OFF since 2026-08-16** — all four off-peak CronJobs suspended via `chatterbox_scheduling_enabled=false`; the demand gate had never seen queued work and the nightly 02:00-06:00 window was holding a T4 slice with an empty queue. Deployment/image/config untouched; flip the variable to restore. Sablier is moot while it never scales up).
- **Docker images**: Always build for `linux/amd64`. SHA-tag rule is being phased out — see `docs/plans/2026-05-16-auto-upgrade-apps-{design,plan}.md`. New model: CI pushes `:latest` (optionally also `:<8-char-sha>` for traceability), Keel polls and triggers rollouts. Cache-staleness concern from the old rule is resolved at the nginx layer (URL-split — manifests pass through, blobs cached). Until Phase 1 of the migration completes (per the plan), follow the SHA-tag rule for new services to match existing pattern. **NEVER set `keel.sh/policy=force` on a deployment tracking an upstream multi-tag repo** — force deploys whatever tag Keel's poll picks regardless of semver ordering and rolled paperless-ngx from `:2.20.15` to the ancient `:1.5.0` within minutes (2026-07-14, memory #9838). House convention for pinned-version images is `keel.sh/policy=patch`; recover a bad roll with `kubectl set image` (image is the Keel-managed field, `ignore_changes` in TF).
- **Image registry**: **Owned images now live on `ghcr.io/viktorbarzin/<name>`** (ADR-0002, built by GHA — see the CI/CD Architecture section). The **Forgejo container registry is FROZEN + emptied** (break-glass only — `docs/runbooks/forgejo-registry-breakglass.md`); nothing pushes to it. The rest of this bullet documents the **still-live forgejo-pull DNS/mirror machinery** (it remains in place for the break-glass path + because `registry-credentials` is still Kyverno-synced; the hairpin lessons apply to any internal-registry pull). Historical usage was `image: forgejo.viktorbarzin.me/viktor/<name>:<tag>` + `imagePullSecrets: [{name: registry-credentials}]`. **Kubelet pulls** are kept off the hairpin **at the resolver, with zero node-side DNS config**: pfSense Unbound carries a domain override forwarding the whole `viktorbarzin.me` zone to Technitium (added 2026-06-10, `docs/runbooks/pfsense-unbound.md`), whose split-horizon zone CNAMEs every ingress host (auto-synced hourly by `technitium-ingress-dns-sync`) to the zone apex whose A record tracks the **live** Traefik LB IP (canary: the `apex-dns` blackbox scrape — a `dns_apex` module asking Technitium for the apex A record and failing unless it is the live LB IP; alerts ViktorBarzinApexDrift. Replaced the `viktorbarzin-apex-probe` CronJob on 2026-08-16, which pip-installed dnspython on each of 288 daily runs). Nodes are stock — link DNS `10.0.20.1 94.140.14.14` via `qm set --nameserver`, no `/etc/hosts` pins, no resolved drop-ins (two same-day interim approaches on 2026-06-10 were removed the same day). The containerd `hosts.toml` mirror (`[host."https://10.0.20.203"]`, `skip_verify = true`) still exists but is **vestigial** — it can NOT keep pulls internal on its own: Traefik routes by Host/SNI and 404s the mirror's bare-IP requests, and the registry's Bearer auth realm is the absolute `https://forgejo.viktorbarzin.me/v2/token` URL fetched outside the mirror — without internal DNS every fresh pull degrades to public DNS → hairpin → intermittent `dial tcp 176.12.22.76:443: i/o timeout` ImagePullBackOff (tuya-bridge 7.5h outage 2026-06-10, tripit 2026-06-09; see `docs/post-mortems/2026-06-10-tuya-bridge-forgejo-pull-hairpin.md`). **In-cluster pods are ordinary internal clients too** (since 2026-06-10 evening) — CoreDNS's dedicated `viktorbarzin.me:53` block (Corefile in `stacks/technitium/modules/technitium/main.tf`) forwards to the Technitium ClusterIP `10.96.0.53`, so pods get the same split-horizon answers as everyone else; forgejo stays pinned to Traefik's **ClusterIP** in that block (TF-interpolated from the live Service) so CI pushes survive a Technitium outage. This relies on a k8s-1.34 behavior verified 2026-06-10: **pods CAN reach the ETP=Local Traefik LB IP** (kube-proxy short-circuits in-cluster traffic to LB IPs via the cluster path) — re-verify after major k8s upgrades; canary = the uptime-kuma `[External]` fleet going red. (The block briefly forwarded to `8.8.8.8/1.1.1.1` earlier that day, which kept pods on the WAN IP and the broken TP-Link NAT loopback — 27 non-proxied `[External]` monitors dark; beads code-yh33.) **Was `.200` until 2026-06-01** — Traefik's 2026-05-30 move to its dedicated `.203` left the mirror pointing at the now-dead `.200:443`, silently breaking every *fresh* forgejo pull; a future LB renumber is now handled by DNS (apex record + drift probe) — only the vestigial hosts.toml literal would go stale. Mirror source lives in `modules/create-template-vm/k8s-node-containerd-setup.sh` (new nodes) and `scripts/setup-forgejo-containerd-mirror.sh` (existing nodes; also cleans up the legacy 2026-06-10 node-DNS customization). Push-side: viktor PAT in Vault `secret/ci/global/forgejo_push_token` (Forgejo container packages are scoped per-user; only the package owner can push, ci-pusher cannot write to viktor/*). Pull-side: cluster-puller PAT in Vault `secret/viktor/forgejo_pull_token`. Retention CronJob (`forgejo-cleanup` in `forgejo` ns, daily 04:00) keeps newest 10 versions + always `:latest` + any buildkit `*cache*` tag — **REVERTED to DRY_RUN 2026-06-10 after its first live run orphaned OCI index children** (multi-arch/attestation children are separate *untagged* sha256 versions that sort outside the newest-10 window while their parent index is kept; broke `kms-website:latest`+`:dfc83fb`, caught by the integrity probe, healed by re-tagging latest→a794d1a + deleting the corrupt version; see `docs/post-mortems/2026-06-10-forgejo-retention-orphaned-indexes.md`). Do NOT re-enable deletes until the keep-set resolves kept indexes' child digests (or skips untagged versions, or moves to Forgejo's native container-aware cleanup rules). The registry PVC remains at its 50Gi autoresize ceiling on the HDD (we did NOT move it to SSD, see beads code-oflt), so a container-aware retention is still needed. Integrity probed every 15min by `forgejo-integrity-probe` in `monitoring` ns (catalog walk + manifest HEAD on every blob). See `docs/plans/2026-05-07-forgejo-registry-consolidation-{design,plan}.md` for the migration history. Pull-through caches for upstream registries (DockerHub, GHCR, Quay, k8s.gcr, Kyverno) stay on the registry VM at `10.0.20.10` ports 5000/5010/5020/5030/5040 — the old port-5050 R/W private registry was decommissioned 2026-05-07.
- **Repowise / the Corpus (ADR-0024)**: `repowise-mcp.viktorbarzin.me/mcp` (internal-only) answers architecture, dependency-graph, git-hotspot, ownership, code-health, dead-code and blast-radius questions across **all 42 first-party Forgejo repos**. Wire it per-Workstation with `claude mcp add --transport http repowise <url> --header "Authorization: Bearer <token from Vault secret/repowise → bearer_tokens>"` (`~/.claude.json` is per-user mutable state — never shared, so this is not done centrally). **What it answers is Forgejo MASTER, as last indexed, up to ~1h behind — never your worktree.** It cannot see your branch or your uncommitted edits, and the file excerpts it returns come from the cluster's checkout of master: **never quote a repowise excerpt as current code** — use Read/Grep for that. Its own `stale_warning` will NOT warn you about the ~1h lag (it compares the index against the local clone, and the reconciler reindexes immediately after fetching, so they stay in lockstep whatever Forgejo has moved on to). Adding a repo to the Corpus needs no action: create it under Forgejo `viktor/*` with a commit and the next hourly pass indexes it; the Corpus is the rule "not archived, not empty", not a list. GitHub-only repos (`health`, `learning`, `interview-prep-app`) are NOT indexed — mirror to Forgejo to include one. As-built + troubleshooting: `docs/architecture/repowise.md`.
- **LinuxServer.io containers**: `DOCKER_MODS` runs apt-get on every start — bake slow mods into a custom image (`RUN /docker-mods || true` then `ENV DOCKER_MODS=`). Set `NO_CHOWN=true` to skip recursive chown that hangs on NFS mounts.
- **Node memory changes**: When changing VM memory on any k8s node, update kubelet `systemReserved`, `kubeReserved`, and eviction thresholds accordingly. Config: `/var/lib/kubelet/config.yaml`. Template: `stacks/infra/main.tf`. Current values: systemReserved=512Mi, kubeReserved=512Mi, evictionHard=500Mi, evictionSoft=1Gi.
- **Node OS disk tuning** (cloud-init in `modules/create-template-vm/cloud_init.yaml`; kubelet/ext4 also in `stacks/infra/main.tf`): kubelet `imageGCHighThresholdPercent=70` (was 85), `imageGCLowThresholdPercent=60` (was 80), ext4 `commit=60` in fstab (was default 5s). **journald `Storage=volatile` (RAM, `RuntimeMaxUse=200M`) since 2026-06-30 (code-oflt)** — node+pod journals ship to Loki via alloy (`loki.source.journal`), so on-disk journald was pure write-IOPS overhead on the IOPS-bound `sdc`; moving it to RAM cut node5's OS-disk writes ~94% (`jbd2` 931→46 KB/s) and freed ~4 GB each on node4/node5 (which had drifted to uncapped persistent). Applied to running nodes via `qm guest exec` drop-in `/etc/systemd/journald.conf.d/90-oflt-volatile.conf` + journald restart. Trade-off: a hard crash loses the last unshipped journal (Loki has the rest).
- **Sealed Secrets**: User-managed secrets go in `sealed-*.yaml` files in the stack directory. Stacks pick them up via `kubernetes_manifest` + `fileset(path.module, "sealed-*.yaml")`. See AGENTS.md for full workflow.
- **CRITICAL — Update docs with every change**: When modifying infrastructure (Terraform, Vault, networking, storage, CI/CD, monitoring), you MUST update all affected documentation in the same commit. Check and update: `docs/architecture/*.md`, `docs/runbooks/*.md`, `.claude/CLAUDE.md`, `AGENTS.md`, `.claude/reference/service-catalog.md`. Stale docs cause incident response failures and onboarding confusion. If unsure which docs are affected, grep for the service/resource name across all doc files.

## Terraform State — Two-Tier Backend
- **Tier 0 (bootstrap)**: Local state, SOPS-encrypted in git. Stacks: `infra`, `platform`, `cnpg`, `vault`, `dbaas`, `external-secrets`. These must exist before PG is reachable.
- **Tier 1 (everything else)**: PostgreSQL backend (`pg`) on CNPG cluster at `pg-cluster-rw.dbaas.svc.cluster.local:5432/terraform_state`. Native `pg_advisory_lock` for concurrent safety. Each stack gets its own PG schema. **Lock contention is non-fatal**: `scripts/tg` passes `-lock-timeout` (default `5m`) so a contended lock waits rather than hard-failing — this was the #1 cause of infra CI failures (a Woodpecker-killed run's unreaped PG lock, a concurrent local apply, or the daily drift `plan`; Tier-1 stacks have no Vault advisory-lock skip to fall back on, unlike Tier-0).
- **Auth**: `scripts/tg` auto-fetches PG credentials from Vault (`database/static-creds/pg-terraform-state`). Humans use `vault login -method=oidc`, agents use K8s auth (role: `terraform-state`, namespace: `claude-agent`).
- **Tier 0 workflow** (unchanged): `git pull` → `scripts/tg plan` → `scripts/tg apply` → `git push`. State sync via SOPS is transparent.
- **Tier 1 workflow**: `vault login -method=oidc` → `scripts/tg plan` → `scripts/tg apply`. No git commit needed — PG is authoritative.
- **Tier detection**: Defined in `terragrunt.hcl` (`locals.tier0_stacks`), `scripts/tg`, and `scripts/state-sync`. All three share the same list.
- **Fallback**: If PG is down, Tier 0 local state can bring it back (`scripts/tg apply` in `dbaas` stack). Tier 1 ops are blocked until PG recovers.
- **Tier 0 details**: Decrypt priority: Vault Transit (primary) → age key fallback. Encrypt: both Vault Transit + age recipients. Scripts: `scripts/state-sync {encrypt|decrypt|commit} [stack]`.
- **Adding operator**: Generate age key (`age-keygen`), add pubkey to `.sops.yaml`, run `sops updatekeys` on Tier 0 `.enc` files. For Tier 1, only Vault access is needed.
- **Migration script**: `scripts/migrate-state-to-pg` (one-shot, idempotent) migrates Tier 1 stacks from local to PG.
- **Adopting existing resources**: use HCL `import {}` blocks (TF 1.5+), not `terraform import` CLI. Commit stanza → plan-to-zero → apply → delete stanza. Canonical reason: reviewable in PR, plan-safe, idempotent, tier-agnostic. Full rules + per-provider ID formats in `AGENTS.md` → "Adopting Existing Resources".

## Secrets Management — Vault KV
- **Vault is the sole source of truth** for secrets.
- **`secret/viktor`** — go-to path for ALL personal secrets (135 keys). Contains every API key, token, password, SSH key, and config from the old terraform.tfvars. Check here first: `vault kv get -field=KEY secret/viktor`.
- **Auth**: `vault login -method=oidc` (Authentik SSO) → `~/.vault-token` → read by Vault TF provider.
- **Vault stack self-reads**: `data "vault_kv_secret_v2" "vault"` reads its own OIDC creds from `secret/vault`.
- **ESO (External Secrets Operator)**: `stacks/external-secrets/` — chart **2.6.0 / app v2.6.0** (migrated 0.12.1→2.6.0 on 2026-06-22, one minor at a time; helm_release has `atomic=true`). **~104 ExternalSecrets across 73 files**, all on **API version `v1`** (migrated v1beta1→v1 on 2026-06-22 — there is NO v1beta1→v1 conversion webhook, so all CRs were rewritten to v1 on chart 0.16.2 before 0.17 removed v1beta1; see `docs/plans/2026-06-21-eso-0.12-to-2.x-migration-design.md`). Two ClusterSecretStores: `vault-kv` and `vault-database`. (1 pre-existing dead ES — `instagram-poster-secrets` — fails "cannot find secret data" on a missing Vault key, unrelated. `payslip-ingest-secrets` was in that state too and is **no longer**: verified `SecretSynced True` with all 6 keys on 2026-08-16, so re-check before citing an ES as dead.)
- **Plan-time pattern**: Former plan-time stacks use `data "kubernetes_secret"` to read ESO-created K8s Secrets at plan time (no Vault dependency). First-apply gotcha: must `terragrunt apply -target=kubernetes_manifest.external_secret` first, then full apply. `count` on resources using secret values fails — remove conditional counts.
- **14 hybrid stacks** still keep `data "vault_kv_secret_v2"` for plan-time needs (job commands, Helm templatefile, module inputs). Platform has 48 plan-time refs — no migration possible without restructuring modules.
- **Database rotation**: Vault DB engine rotates passwords every 7 days (604800s). MySQL: speedtest, wrongmove, codimd, nextcloud, shlink, grafana, phpipam. PostgreSQL: health, linkwarden, affine, woodpecker, claude_memory, crowdsec, technitium. Excluded: authentik (PgBouncer), root users. **Apps that read a rotated secret only at startup** (env var / initContainer, not a hot-reloaded mount) MUST carry a Reloader annotation (`secret.reloader.stakater.com/reload: <secret>`) or they keep the stale password and silently fail DB auth on each rotation until manually restarted — matrix's Synapse `inject-db-password` initContainer hit exactly this (found via Loki 2026-06-05, ~12.9k auth-fail lines/hr); matrix has since migrated to tuwunel (RocksDB, no Postgres) on 2026-06-08 and is no longer in the rotation list above. Technitium uses a password-sync CronJob (every 6h) to push rotated password to the Technitium app config via API, disable SQLite + MySQL logging, check PG plugin is loaded, configure PG query logging (90-day retention), and disable SQLite on secondary/tertiary instances.
- **K8s credentials**: Vault K8s secrets engine. Roles: `dashboard-admin`, `ci-deployer`, `openclaw`, `local-admin`. Use `vault write kubernetes/creds/ROLE kubernetes_namespace=NS`. Helper: `scripts/vault-kubeconfig`.
- **CI/CD (GHA + Woodpecker)**: Docker builds run on **GitHub Actions** (free on public repos). Woodpecker is **deploy-only** — receives image tag via API POST, runs `kubectl set image`. Woodpecker authenticates via K8s SA JWT → Vault K8s auth. Sync CronJob pushes `secret/ci/global` → Woodpecker API every 6h. Shell scripts in HCL heredocs: escape `$` → `$$`, `%{}` → `%%{}`.
- **Platform cannot depend on vault** (circular). Apply order: vault first, then platform. Platform has 48 vault refs, all in module inputs — no ESO migration possible.
- **Complex types** (maps/lists like `homepage_credentials`, `k8s_users`) stored as JSON strings in KV, decoded with `jsondecode()` in consuming stack `locals` blocks.
- **New stacks**: Add secret in Vault UI/CLI at `secret/<stack-name>`, add ExternalSecret + `data "kubernetes_secret"` for plan-time, `secret_key_ref` for env vars. Use `data "vault_kv_secret_v2"` only if `data "kubernetes_secret"` won't work (e.g., first-apply bootstrap).
- **Backup CronJob**: `vault-raft-backup` uses manually-created `vault-root-token` K8s Secret (independent of automation).
- **Bootstrap (fresh cluster)**: Comment out data source + OIDC → apply Helm → init+unseal → populate `secret/vault` → uncomment → re-apply.

## Resource Management Patterns
- **CPU**: All CPU limits removed cluster-wide (CFS throttling). Only set CPU requests based on actual usage.
- **Memory**: Set explicit `requests=limits` based on VPA upperBound. Target: upperBound x 1.2 for stable services, x 1.3 for GPU/volatile workloads.
- **Right-sizing**: VPA/Goldilocks was **REMOVED 2026-06-12** (etcd-load-reduction — 349 VPAs all ran `updateMode=Off`, costing ~800 etcd objects + continuous recommender writes + a pod-creation admission webhook for dashboard-only value). Right-size **on demand with `krr`** (Robusta, Dockerized from the devvm — no cluster install, no admission webhook, no eviction risk; reads Prometheus). Set container resources explicitly in TF from krr output.
- **LimitRange**: Tier-based defaults silently apply to pods with `resources: {}`. Always set explicit resources on containers needing more than defaults. Tier 3-edge and 4-aux now use Burstable QoS (request < limit) to reduce scheduler pressure.
- **Democratic-CSI sidecars**: Must set explicit resources (32-80Mi) in Helm values — 17 sidecars default to 256Mi each via LimitRange. `csiProxy` is a TOP-LEVEL chart key, not nested under controller/node.
- **ResourceQuota blocks rolling updates**: When quota is tight, scale to 0 then back to 1 instead of RollingUpdate. Or use Recreate strategy.
- **Kyverno ndots drift**: Kyverno injects dns_config on all pods. Every `kubernetes_deployment`, `kubernetes_stateful_set`, and `kubernetes_cron_job_v1` MUST include `lifecycle { ignore_changes = [spec[0].template[0].spec[0].dns_config] # KYVERNO_LIFECYCLE_V1 }` (use `spec[0].job_template[0].spec[0].template[0].spec[0].dns_config` for CronJobs). The `# KYVERNO_LIFECYCLE_V1` marker is the canonical discoverability tag — grep for it to locate every site. A shared Terraform module was considered but `ignore_changes` only accepts static attribute paths (not module outputs, locals, or expressions), so the snippet convention is the only viable path. Full rationale and copy-paste snippets in `AGENTS.md` → "Kyverno Drift Suppression".
- **NVIDIA GPU operator resources**: dcgm-exporter and cuda-validator resources configurable via `dcgmExporter.resources` and `validator.resources` in nvidia values.yaml.
- **Pin database versions**: Disable Diun (image update monitoring) for MySQL, PostgreSQL, Redis.
- **Quarterly right-sizing**: Run `krr` (Dockerized, against Prometheus) for recommendations; compare to current requests and adjust in TF. (Goldilocks dashboard removed 2026-06-12.)

## CI/CD Architecture — GHA Builds → ghcr + Woodpecker Deploy

**Doctrine (ADR-0002, fleet-wide as of 2026-06-13): ALL image builds + CI
compute run OFF-infra.** Every owned image is built/linted/tested on GitHub
Actions (public repos: free; private: 2000 free min/mo) and pushed to
`ghcr.io/viktorbarzin/<name>`. **No in-cluster image builds or CI test runs
exist anywhere** — the in-cluster Woodpecker buildkit and the fallback-build
pattern were removed (clean cut). Woodpecker is **deploy-only** (plus infra
applies + maintenance crons). Canonical CI/CD reference:
`docs/architecture/ci-cd.md`; decision: `docs/adr/0002-all-image-builds-off-infra-gha-ghcr.md`.
**Watch what you trigger**: after a push that fires a build chain, follow it to
completion (GHA run → Woodpecker deploy → `rollout status`) and fix failures;
verify via live state, not the checkmark.

**The fleet pattern (every owned app):** Forgejo `viktor/<repo>` (canonical)
push-mirrors (`sync_on_commit`) → GitHub `ViktorBarzin/<repo>` → GHA
`.github/workflows/build.yml` (committed on Forgejo, mirrors over): `on: push:
branches:[master]` ONLY (feature branches mirror but build/deploy nothing — the
safety valve). The `build` job: lint/test → `svu` cuts the next `vX.Y.Z` tag to
CANONICAL Forgejo (GHA secret `FORGEJO_GIT_TOKEN` = write:repository PAT) + bakes
`VERSION` → `buildx` `linux/amd64` `provenance:false` (single-manifest, dodges
the orphaned-index-children class) → push `ghcr.io/viktorbarzin/<name>:<sha8>` +
`:latest` → `delete-package-versions` keep-10. The `deploy` job POSTs
`ci.viktorbarzin.me/api/repos/<id>/pipelines` (the GitHub-mirror's Woodpecker
registration, github-forge; GHA secret `WOODPECKER_TOKEN`) with `IMAGE_TAG` +
`IMAGE_NAME` → `.woodpecker/deploy.yml` (event:**manual** ONLY, so the raw
Forgejo→GitHub mirror pushes don't fire a tag-less deploy) runs `kubectl set
image deployment/<app> …` in-cluster (woodpecker-agent SA = cluster-admin, no
kubeconfig). Deployment image is `ignore_changes`/KEEL_IGNORE_IMAGE so the SHA
sticks vs `terragrunt apply`; CronJobs track `:latest` + `imagePullPolicy:
Always`. **Keel stays enrolled** as a redundant net (sees the SHA already
running → no-op). **Never** `set image`/`rollout restart` operator-managed
StatefulSets (memory id=740). Onboarding tool: `scripts/offinfra-onboard` +
`scripts/offinfra-templates/`; mirror + workflow commits via the Forgejo API over
the internal Traefik LB (`curl --resolve forgejo.viktorbarzin.me:443:10.0.20.203`).
Reference impls: tripit (the original pilot), f1-stream, job-hunter, tuya_bridge.

**Migrated apps (issues #13–#27):** f1-stream, job-hunter, tuya_bridge,
beadboard, nextcloud-todos, claude-agent-service, **claude-memory-mcp** (GHA →
ghcr, NOT DockerHub), kms-website, Freedify, instagram-poster, payslip-ingest,
broker-sync (image `wealthfolio-sync`), fire-planner, recruiter-responder,
x402-gateway — plus tripit. Earlier public-repo apps already on GHA (Website,
apple-health-data, audiblez-web, plotting-book, insta2spotify,
audiobook-search) now also land on ghcr.
- **PUBLIC ghcr packages:** beadboard, nextcloud-todos, claude-agent-service,
  claude-memory-mcp, kms-website, freedify, tuya_bridge, x402-gateway,
  android-emulator.
- **PRIVATE ghcr:** f1-stream, job-hunter, instagram-poster, payslip-ingest,
  wealthfolio-sync, fire-planner, recruiter-responder, tripit, infra-cli,
  infra-ci, k8s-portal, excalidraw-library, chesscom-streak. Pulled via the Kyverno-synced `ghcr-credentials` allowlist
  (`stacks/kyverno/modules/kyverno/ghcr-credentials.tf`; NOT cluster-wide; cred
  = Vault `secret/viktor/ghcr_pull_token`, a dedicated classic PAT scoped to
  `read:packages` (UI-minted 2026-06-15; no longer the admin `github_pat`
  alias). GitHub has no token-mint API, so rotation is manual: re-mint →
  `vault kv patch secret/viktor ghcr_pull_token=…` → targeted apply
  `module.kyverno.kubernetes_secret.ghcr_credentials` (reads Vault, dodges the
  git-crypt tls-secret-sync landmine), Kyverno re-syncs the allowlist).

**Infra-owned images (issues #29/#30)** build on GHA workflows IN the infra
repo's own `.github/workflows/` (added to the GitHub lineage via PR; the
github↔forgejo divergence was deliberately NOT reconciled):
`build-android-emulator.yml` → public ghcr;
`build-cli.yml` → DockerHub `viktorbarzin/infra` (kept) + `ghcr.io/viktorbarzin/infra-cli`;
`build-infra-ci.yml` → `ghcr.io/viktorbarzin/infra-ci`; `build-k8s-portal.yml` →
PRIVATE `ghcr.io/viktorbarzin/k8s-portal` (Keel-deployed; the LAST in-cluster
Woodpecker build, migrated 2026-06-13 — completes "no local builds"); `build-excalidraw.yml` →
PRIVATE `ghcr.io/viktorbarzin/excalidraw-library` (Keel-deployed; replaced
manual DockerHub pushes 2026-07-02 — DockerHub `:v4` frozen as rollback). **infra-ci**
is the image the `.woodpecker/default.yml` apply step + `drift-detection.yml` run
in (proven by pipelines 165/166). chatterbox-tts is already built by tripit's GHA → ghcr.
The Woodpecker `build-ci-image.yml` + `build-cli.yml` pipelines were REMOVED;
infra-ci break-glass is a manual `.woodpecker/breakglass-infra-ci.yml` (ghcr
pull-and-save to the registry VM).

**Forgejo container registry: FROZEN + emptied** (issue #32 wiped all `viktor/*`
container packages). Break-glass-only now; nothing pushes. `forgejo-cleanup`
stays DRY_RUN. Pull-through caches on `10.0.20.10` are unchanged. Runbook:
`docs/runbooks/forgejo-registry-breakglass.md`.

**Woodpecker now runs only:** per-app `deploy.yml` (manual, `kubectl set
image`), `default.yml` (terragrunt apply), `renew-tls.yml` (certbot),
maintenance crons (drift-detection, provision-user, registry-config-sync,
pve-nfs-exports-sync, postmortem-todos), and the
manual `breakglass-infra-ci.yml`. **No build/test pipeline on any repo — do not
(re)introduce one.** (`.woodpecker/k8s-portal.yml`, the last in-cluster image
build, was removed 2026-06-13 — k8s-portal now builds on GHA → ghcr, see
Infra-owned images above.)

**Decommissioned (issue #31):** travel_blog (stack destroyed + dir removed), 6
dead builders' pipelines (terminal-lobby, webhook-handler, hmrc-sync,
trading-bot, travel-agent, trip-planner), and all `build-fallback.yml` files
(only Website had one).

**Woodpecker API**: numeric repo IDs (`/api/repos/<id>/pipelines`), NOT
owner/name (those return HTML). The deploy registration for each app is the
**GitHub mirror** repo (github-forge). Infra: Forgejo forge = repo 82, legacy
GitHub forge = repo 1.

**Woodpecker YAML gotchas**:
- Commands with `${VAR}:${VAR}` must be **quoted** — unquoted `:` triggers YAML map parsing when vars are empty
- Use `bitnami/kubectl:latest` (not pinned versions — entrypoint compatibility issues)
- Global secrets must have `manual` in their events list for API-triggered pipelines

**GitHub repo secrets** (per repo): `WOODPECKER_TOKEN` (POST deploy pipeline),
`FORGEJO_GIT_TOKEN` (write:repository PAT for the svu tag push). ghcr push uses
the workflow's built-in `GITHUB_TOKEN` (`packages: write`).

## Database Host

**`postgresql_host`** in `config.tfvars` is `pg-cluster-rw.dbaas.svc.cluster.local` (the CNPG primary). The legacy `postgresql.dbaas` service is a live compatibility alias (selector `cnpg.io/instanceRole=primary`, so it also reaches the primary — authentik's PgBouncer still points at it) — but use `pg-cluster-rw` for anything new. This variable is shared by ~12 stacks.

**CNPG tuning** (in `stacks/dbaas/modules/dbaas/main.tf`): `shared_buffers=1024MB`, `effective_cache_size=2560MB`, `work_mem=16MB`, `max_connections=200`, pod memory 3Gi. **Write-reduction (2026-06-29, code-oflt, analysis #6922):** `checkpoint_timeout=15min` + `max_wal_size=4GB` + `min_wal_size=1GB` (checkpoints were 100% timer-driven at the 5-min default, bursting FPIs onto sdc); `archive_timeout=0` (CNPG forces `archive_mode=on` but `.spec.backup` is empty → a 16MB WAL switch every 300s shipped nowhere = ~4.6 GB/day waste; daily `pg_dump` is the real backup); `commit_delay=2500`µs (group-commit fsync coalescing, safe for all DBs incl financial); `wal_compression=zstd` (was pglz). All reloadable (no restart). **`shared_preload_libraries: [pg_prewarm]` + `pg_prewarm.autoprewarm=on`/`autoprewarm_interval=300` (2026-09-02, infra#86)** — postmaster-level, so unlike everything above it costs ONE rolling restart to change (`unsupervised`/`restart`, replicas first); removing an entry later needs a full restart of every instance. It exists because **a NOVEL query, not an idle period, is what pays for a cold index**: measured on claude-memory's HNSW index, a repeated query read 0 blocks from disk at 0s/30s/60s/120s idle, while a query with a new embedding read 199-491 blocks off sdc at ~8.9ms each (1.25-2.48s) because only 339 of the index's 4,456 pages were ever resident. Postgres caches pages on demand, so a 35MB index in a 1GB `shared_buffers` is **not** implicitly resident — it is resident only where someone has already walked. `pg_prewarm` loads all of it (0 blocks, 0.27-0.49s afterwards) and autoprewarm makes that survive a restart. **Do not diagnose a slow first query as an eviction problem before checking `pg_buffercache` residency** — a decay measurement that varies its query text measures query-to-query path variance, not decay, and reads as noise. The same pattern has run on immich's own (separate, non-CNPG) Postgres since its `clip_index` work. **Apply gotcha:** the Cluster is a `null_resource.pg_cluster` + local-exec `kubectl apply` — bump its `pg_params` trigger or the YAML edit is inert, and apply with `-target=module.dbaas.null_resource.pg_cluster` for fast iteration. (The `mysql_standalone` VCT-annotation drift that used to error broad `dbaas` applies was **fixed 2026-06-30** — `ignore_changes` on the STS `volume_claim_template`, since pvc-autoresizer owns PVC sizing and the VCT is immutable post-creation anyway.)

## Networking & Resilience
- **Critical path services scaled to 3**: Traefik, Authentik, CrowdSec LAPI, PgBouncer, Cloudflared.
- **PDBs**: minAvailable=2 on Traefik and Authentik.
- **Fallback proxies**: basicAuth when Authentik is down, fail-open when poison-fountain is down.
- **CrowdSec enforcement has TWO surfaces, split by what can identify the client** (pivoted 2026-08-18 off the Cloudflare edge): (1) **all HTTP, proxied included** → the in-process **`crowdsec` Traefik plugin** on the **`websecure` ENTRYPOINT** (`stacks/traefik/modules/traefik/crowdsec-bouncer-plugin/` + the Middleware in that module's `middleware.tf`). Entrypoint, not per-ingress, so it is prepended to every router on it — all ~195 Ingresses, the 10 IngressRoutes and the catchall, including the hand-rolled ones that bypass `ingress_factory`; a `modules/` change would otherwise fan across 33 platform + ~95 app stacks applied serially, where lock-contended stacks are SKIPPED not failed. Polls LAPI `/v1/decisions` every 30s into an in-memory set, one map lookup per request, 403 on a hit. **Client IP comes from the unspoofable TCP peer** — `Cf-Connecting-Ip`/`X-Forwarded-For` honoured ONLY from `trustedProxyCIDRs` (`10.10.0.0/16`, cloudflared); any other peer is judged on itself, because pfSense NATs WAN :443 straight to Traefik so a banned client can connect directly and claim to be anyone. **Ban-only** (captcha ignored — the `captcha_remediation` profile diverts 4 FP-prone scenarios to a captcha enforced NOWHERE; honouring it would make all 4 live fleet-wide blocks), handles `scope=Range`, **CAPI EXCLUDED** via the `origins` config, auth hosts (`authentik`, `public-auth`) never gated, `dryRun` flag. **Consequence: the enforced set is currently 4 decisions** — this is new coverage for web traffic, not a like-for-like replacement of a 22.7k blocklist. **Fail-open is structural** (no backend to be unreachable; LAPI down → last known set, never loaded → allow) — which is WHY it is in-process and not a ForwardAuth: `forward.go` answers 500/502 on an unreachable auth backend with no allow option (the reason `auth-proxy`/`bot-block-proxy` exist as shims), and a ForwardAuth peer is always a Traefik pod so peer-trust is unimplementable there. Bouncer key `traefik`. (2) **direct hosts + non-HTTP ports** → `cs-firewall-bouncer` DaemonSet drops **in-kernel via nftables** in BOTH the `input` and `forward` hooks (Traefik is ETP=Local so client traffic is DNAT'd to the pod via `forward`); pulls ALL decisions incl. the CAPI blocklist (22,715 of 22,719 live on 2026-08-18). **This covers almost no public web traffic** — `cloudflare_proxied_names = []` so every HTTP host is proxied and the node only sees 10.10.x.x. **The whitelist is PARSER-stage: a manual `cscli decisions add` on an internal IP IS enforced in-kernel on every node** — banning 10.0.10.10 blackholed the devvm incl. its DNS (2026-08-18; `decisions delete` recovered in ~3s). Test with `192.0.2.1`, not your own address. **Unban now takes ~33s** (one poll) vs a ~72h floor at the edge. **Cloudflare edge channel RETIRED 2026-08-18**: the Lists API holds a hard ~72h floor between successful item writes (audit log, full list history: 8 of 16 intervals in [72h, 72h+120s), three at exactly 259,200s, nothing between 4.5h and 72h; rejected attempts do NOT consume it), which left the edge list disagreeing with LAPI for **107 of 216 observed hours**; a hand-ban on our own WAN egress reached the edge 2 days later and blocked every proxied host. List + token + `crowdsec-cf-sync` CronJob + `lapi_kv_sync.py` + the `kvsync` key (and its 442 leaked LAPI rows) all deleted. **`cloudflare_ruleset.crowdsec` was deliberately NOT destroyed** — it is the zone's `default` `http_request_firewall_custom` PHASE ENTRYPOINT, so a DELETE would wipe the zone's whole custom-rules phase; it was reduced to its pre-existing disabled `skip` rule and detached with `removed { lifecycle { destroy = false } }` (the repo's first `removed` block). CF managed DDoS L7 + Bot Fight Mode are independent and untouched. Alerts followed the enforcement: the 3 `CrowdSecEdge*` rules are gone, replaced by `CrowdSecL7BouncerNotPolling` (Prometheus, off `cs_lapi_bouncer_requests_total{bouncer="traefik"}`, guarded on LAPI being up since the series goes ABSENT not flat), plus Loki `CrowdSecL7BouncerRefreshFailing` and `CrowdSecL7BlockBurst` (the latter guards the new feedback loop: blocked requests now reach Traefik and are access-logged, so mass 403s can trip `http-403-abuse`). The plugin runs under Yaegi where Prometheus counters are not cheap, so decisions are log lines `[crowdsec-bouncer] action=...` on traefik stdout. **BEFORE APPLYING ANY vendored-plugin change run `scripts/yaegi-plugin-gate`** — one broken plugin disables ALL Traefik plugins incl. `api-token-middleware` (paperless-mcp, repowise), and `go build`/`go test` do NOT catch it: this plugin passed 26 tests while panicking Yaegi at import because it held a **variadic func in a STRUCT FIELD** (fine as a parameter or package-level func, fatal in a field). Full as-built: `docs/architecture/security.md`; manual bans: `docs/runbooks/crowdsec-manual-bans.md`.
- **Rate limiting**: Return 429 (not 503). Per-service tuning via dedicated middleware + `skip_default_rate_limit` (default 10/s burst 50): Immich 1000/20000, ActualBudget 50/300 (app boot = ~70 parallel revalidations), authentik 100/1000 on `/`+`/static` (login SPA cold-loads ~70 flow chunks from `/static`; default burst 429'd them → blank login screen).
- **Retry middleware**: 2 attempts, 100ms — in default ingress chain.
- **Entrypoint transport timeouts** (`websecure` `respondingTimeouts`): `writeTimeout=0` (unlimited download duration), `readTimeout=3600s` (uploads ≤1h), `idleTimeout=600s`. These are **HARD total-duration caps**, not nginx-style per-read idle timeouts — a finite `writeTimeout` truncates *any* large download at that wall-clock mark (a prior `writeTimeout=60s` silently cut Immich videos at 60s). **Do NOT re-tighten `writeTimeout`**; keep `readTimeout` finite (slow-loris backstop) but ≥ longest expected upload. Full rationale: `docs/architecture/networking.md` → "Entrypoint Transport Timeouts".
- **HTTP/3 (QUIC)**: Enabled on Traefik. Works for **direct (non-proxied) apps** via the dedicated LB IP below (ETP=Local). Proxied apps get QUIC at the Cloudflare edge. **It depends on a NODE sysctl and breaks silently without it (2026-08-29):** Traefik terminates h3 on ONE shared UDP socket per pod, and quic-go's buffer request is capped by `net.core.rmem_max`. A node at the Ubuntu default (`212992`) gives a `425984`-byte socket that overflows on ACK bursts — quic-go loses ACKs, the transfer stalls ~30s and dies, and because Traefik already wrote its `200` + `Content-Length` the access log shows an ordinary success with a short body while the client saves a **truncated file** (a cut JPEG renders correct on top and flat grey below). Measured over 7 days: h3 truncated **54 of 69** large Immich downloads, h2 truncated **0**; after the fix h3 truncations went to zero. `playbooks/k8s-node-tuning.yml` sets `rmem_max`/`wmem_max` = `7500000` (quic-go's documented value) on all 6 nodes; a sysctl change needs a Traefik roll to take effect (quic-go reads the limit once, at socket creation). **The counter is per-NETNS and the node-exporter DaemonSet cannot see it** — it runs `hostNetwork: true` and read 0 for the whole incident while the pod's own counter was 449. Read it with `PID=$(pgrep -x traefik); sudo nsenter -t $PID -n ss -uanpm | grep -A1 ':8443'` (expect `rb=14680064`, `d0`), or rely on the `quic-socket-metrics` sidecar + `TraefikQUICSocketDropping` alert. Full detail: `docs/architecture/networking.md` → "HTTP/3 depends on a node sysctl".
- **Traefik LB IP = `10.0.20.203`, `externalTrafficPolicy: Local`** (dedicated, NOT the shared `.200`). Moved off the shared `.200` on 2026-05-30 so direct/non-proxied apps preserve the **real client IP for CrowdSec** (ETP=Cluster SNAT'd them to the node IP) and so QUIC works. **The shared `10.0.20.200` keeps the other 8 LB services** (PG state-backend `postgresql-lb`, headscale, wireguard, xray, etc. — all ETP=Cluster; MetalLB forbids mixed ETP on a shared IP, hence Traefik's own IP). **coturn took its own `10.0.20.205` (ETP=Local) on 2026-08-11** for the same client-IP reason: on the shared IP, kube-proxy's SNAT made coturn report internal addresses to external clients, breaking every WebRTC relay (both neko browsers stalled at ICE `checking`). pfSense alias `coturn_lb`. **cloudflared targets the in-cluster Traefik Service** (`https://traefik.traefik.svc.cluster.local:443`, remote/dashboard tunnel config — edit via CF Global API Key in `secret/platform`), so proxied apps are decoupled from the LB IP. pfSense WAN 443 (tcp+udp) NAT → alias `traefik_lb` (`.203`). Internal split-horizon apex `viktorbarzin.me A` → `.203`. Full runbook + post-mortem: `docs/plans/2026-05-30-traefik-dedicated-ip-etp-local-*`.
- **IPv6 ingress** = HE 6in4 tunnel (`2001:470:6e:43d::2`) → **standalone HAProxy on pfSense** (`/usr/local/etc/ipv6-haproxy.cfg`, NOT the HAProxy package) using `send-proxy-v2` → Traefik `.203` (web 443/80) + mail NodePorts `30125-30128` (25/465/587/993) — so **real IPv6 client IPs reach CrowdSec**. Traefik trusts PROXY-v2 **only from `10.0.20.1`** (`entryPoints.web/websecure.proxyProtocol.trustedIPs`); real IPv4 clients (own source IP) unaffected. **No QUIC over IPv6** (bridge is TCP/h2). Replaced socat 2026-05-30 (socat masked every v6 client as `10.0.20.1`). Boot/persistence: config.xml `<shellcmd>` → `ipv6_proxy.sh` (rebinds every wildcard `listen [::]:<port>` in the nginx config onto the LAN IPv6 to free the tunnel IPv6, then `service ipv6proxy onestart`); `rc.d/ipv6proxy` manages HAProxy. Backends use **no health `check`** (a plain TCP check false-DOWNs the PROXY-expecting listeners). **HAProxy binds all six frontends or none, so one busy port kills web AND mail over IPv6** — the rebind guard used to test for the literal `[::]:443`, which stopped matching when the webConfigurator moved to 8443; nginx kept wildcard `*:80`, HAProxy could not bind `[TUNNEL]:80`, and the bridge stayed down for 29 days after the 2026-07-18 reboot with nothing alerting (IPv4 fine, Cloudflare-proxied hosts reach the origin over IPv4, so only the ~31 `non-proxied` ingresses + IPv6 mail were dark). The guard is port-agnostic since 2026-08-16 and the script logs whether `[TUNNEL]:443` came up. **Signature of a down bridge:** IPv6 443 = `connection refused`, IPv6 80 = pfSense nginx `301` → `https://<host>:8443/` (its login page + self-signed cert, which a browser follows instead of falling back to IPv4), while `ping6` to the tunnel address still answers. As-built: `docs/architecture/networking.md` → "IPv6 Ingress".
- **IPAM & DNS auto-registration**: pfSense Kea DHCP serves all 3 subnets (VLAN 10, VLAN 20, 192.168.1.x). Kea DDNS auto-registers every DHCP client in Technitium (RFC 2136, A+PTR). CronJob `phpipam-pfsense-import` (hourly) pulls Kea leases + ARP into phpIPAM via SSH (passive, no scanning). CronJob `phpipam-dns-sync` (15min) bidirectional sync phpIPAM ↔ Technitium. 42 MAC reservations for 192.168.1.x.

## Service-Specific Notes
| Service | Key Operational Knowledge |
|---------|--------------------------|
| Nextcloud | MaxRequestWorkers=150, needs 8Gi limit (Apache transient memory spikes, see commit eb94144), very generous startup probe |
| Immich | ML on SSD (CUDA), disable ModSecurity (breaks streaming), frequent upgrades. **`immich-machine-learning` MUST run with `MACHINE_LEARNING_MODEL_TTL > 0`** (set to `600` in `stacks/immich/main.tf`, env on the `immich-machine-learning` deployment). At `0`, no model ever unloads and onnxruntime's CUDA arena (OCR's dynamic input shapes inflate it to ~10 GB) is held forever on the **time-sliced T4 it shares with llama-swap/frigate/immich-server** — which had no VRAM isolation, so immich-ml starved llama-swap (qwen3-8b) and silently broke recruiter-responder triage for ~5 h on 2026-06-02 (post-mortem `docs/post-mortems/2026-06-02-immich-ml-ttl-gpu-oom-recruiter.md`). **Since 2026-07-02 the T4 has a scheduler-level VRAM budget + watchdog (ADR-0016)**: each GPU tenant declares `viktorbarzin.me/gpumem` MiB (per live TF 2026-07-21: immich-worker 3000, llama-swap 5000, frigate 2300, immich-ml 1800, stremio 1500 = 13600; node advertises 14000. **portal-stt was decommissioned 2026-07-21** and its 1500 slot reused by the new **stremio** NVENC transcode server — infra#80, no node-budget bump. **Watchdog fix 2026-07-21 (infra#80):** watchdog.py extracted from the TF heredoc into a real unit-tested file (`watchdog_test.py`) + the `int("5k")` quantity-parse bug fixed — K8s canonicalises `5000`→`"5k"`, so `int()` threw and SILENTLY DROPPED every round-thousand tenant (llama-swap `"5k"`, immich-worker `"3k"`) from the offender set; the parser now handles decimal-SI + binary suffixes) and the `gpu-vram-watchdog` (nvidia ns) recycles the biggest over-budget tenant when free VRAM < 1536 MiB — **DRY_RUN=false since 2026-08-31** (verified live 2026-09-01: the Deployment runs armed with `FLOOR_MIB=1536`, and it is a DEPLOYMENT in ns `nvidia`, NOT a CronJob, so `kubectl -n nvidia get cronjob` alone makes it look absent). It is also CONTENTION-triggered, so it logs `over budget (used=X > YMiB) but nothing is blocked -> allowing the burst, no recycle` and only acts when something is actually starved. **NEVER functional 2026-07-02→07-06; fixed 2026-07-06** — two stacked bugs both masked by broken nvidia-ns DNS (see Known Issues): (1) it reached the apiserver/exporter by DNS name → now uses the always-injected `KUBERNETES_SERVICE_HOST` IP + the exporter's TF-interpolated ClusterIP; (2) its ClusterRole granted only `pods`, never `nodes`, so the first api() call (`GET /nodes`) 403'd → added `nodes` get/list. Now runs a clean tick in DRY_RUN. KNOWN MISCALIBRATION (2026-07-02): llama-swap's real qwen3-8b@16k resident is ~7 GB (the 4.35 GiB figure was weights-only cudaMalloc), so retune budgets (ctx 16k→8k + llama-swap 6144 + immich-ml 2500, or rebalance) BEFORE arming. **LIVE (2026-07-06): with the watchdog finally running, the top over-budget tenant is `immich-ml` (~6750 MiB vs 1800 budget), NOT llama-swap** — it would recycle immich-ml first (TTL=600 → self-reloads); re-check the live `PRESSURE:` log line before arming. **The predicted collision happened 2026-07-07 ~06:25–07:47**: immich-ml's over-budget VRAM left qwen3-8b unable to load; paperless-ai's retry loop made llama-server crash (CUDA OOM) every ~8s, each crash dumping a ~536MiB core into the container writable layer (~148GiB in 50min) → node1 DiskPressure eviction storm (1.8k evicted pods, DNS primary down ~07:28–07:34, K8sMassDelete fired). Mitigated by restarting immich-ml + llama-swap; hardened same day: llama-swap runs `ulimit -c 0` + `ephemeral-storage` request/limit `1Gi/10Gi` (stacks/llama-cpp), so a recurrence kills only the llama-swap pod, not the node. The budget retune above remains the open pre-arming action. TTL>0 lets idle models (OCR, face — AND CLIP) free VRAM. The TTL is a single GLOBAL knob (no per-model pin), so CLIP would also unload after 600s idle; the `warmup` container of the `immich-search-probe` CronJob (`*/5`, same stack; was a standalone `clip-keepalive` job until 2026-08-16) pings the CLIP textual encoder so smart-search stays warm without pinning the ad-hoc models. **Smart search has a SECOND warmth layer in Postgres** (don't conflate it with the ML model): the ~665MB vchord `clip_index` must stay resident in PG `shared_buffers`, else an ANN probe that lands on an evicted list pays a ~1.8s cold storage read vs ~4ms warm. The `postStart` hook prewarms it ONCE at pod start and `pg_prewarm.autoprewarm` only re-warms at *startup*, so the index decays out of cache over days under job buffer-pressure (observed ~33% resident after 9d uptime → slow context search, easily misattributed to the ML model). **One CronJob does all three since 2026-08-16** — `immich-search-probe` (`*/5`) absorbed the former `clip-index-prewarm` and `clip-keepalive` jobs, which fired on the same tick against the same services and cost 864 pod creations/day between them; merging changed no cadence and no latency (864 -> 288/day). Its init container re-runs `pg_prewarm('clip_index')` on the :00/:30 ticks only (48/day, ample against a decay measured in days) and measures live latency + residency → Pushgateway gauges (`immich_smart_search_db_seconds`, `immich_clip_index_cached_pct`) → alerts `ImmichSmartSearchSlow`/`ImmichClipIndexColdCache`/`ImmichSearchProbeStale` + cluster-health check #46 (`check_immich_search`). immich PG role is a superuser so the CronJobs can run `pg_prewarm`/`pg_buffercache`. **Worker split (2026-07-12)**: `immich-api` Deployment (3 GPU-free replicas spread on node2/3/4, serves ALL client traffic incl. websockets — no sticky sessions, Immich ships the socket.io Redis adapter; RollingUpdate maxUnavailable=1/maxSurge=0, PDB minAvailable=2) + `immich-worker` Deployment (single replica = ALL background jobs + transcoding; MUST stay 1 — BullMQ concurrency is per-worker-process, upstream #28051; NO HTTP surface/probes — worker binds an ephemeral port, health = process-alive + `ImmichNoJobWorker` Loki alert on the api pods' checkWorkers() warning). Service `immich-server` kept its name, selector → api pods. Design + rollout: `docs/plans/2026-07-12-immich-horizontal-scaling-design.md`. **Video transcoding is GPU-accelerated**: `immich-worker` (ex `immich-server`) is pinned to GPU node1 (nodeSelector `nvidia.com/gpu.present` + NoSchedule toleration + `gpu-workload` priority) with a time-sliced `nvidia.com/gpu=1` slice — the stock immich-server image's ffmpeg already ships h264/hevc_nvenc + NVDEC. Activated via `ffmpeg.accel=nvenc` + `accelDecode=true` in the **DB** system-config (`system_metadata` table, key `system-config`, JSONB — NOT Terraform; app config is DB-managed here like oauth/smtp). Direct DB edits need a pod **recreate** to reload (config is cached at boot; only API-driven changes broadcast a reload). **Streaming bitrate is capped** to keep 4K playback smooth on the contended HDD and over remote uplinks: `ffmpeg.maxBitrate=20000k` + `preset=medium` + `transcode=bitrate` (set 2026-06-01 — was uncapped `maxBitrate=0` + `ultrafast` + `targetResolution=original`, which produced 77–264 Mbps 4K transcodes that stuttered for every client, local and remote, since even a single stream needs ~10–13.5 MB/s off the shared `sdc` spindle). 4K resolution is preserved (`targetResolution=original`); originals are NEVER modified — only the `encoded-video/` streaming copy. To re-apply transcode settings to EXISTING videos (config changes only affect new/missing ones): delete the offenders' `asset_file` rows `WHERE type='encoded_video'` (derived/regenerable — never touches originals) then run videoConversion `force=false` (admin Jobs API → "Missing"); it regenerates them to the deterministic `<assetId>.mp4` path at concurrency 1 (gentle on sdc). See `docs/runbooks/immich-transcode-bitrate.md`. If Immich is ever reinstalled fresh (not restored), re-set these keys (accel, accelDecode, **maxBitrate=20000k, preset=medium, transcode=bitrate**). Thumbnails/previews live on SSD NFS (sdb) — do NOT move to block storage (HDD sdc = slower + the contended IO domain). **Background-job concurrency is capped to protect sdc** (DB-managed system-config, `system_metadata` key `system-config`, JSONB `job.*.concurrency`; re-set on fresh install): `thumbnailGeneration=2`, `metadataExtraction=2`, `library=2` — these jobs read ORIGINALS off the HDD library. Left uncapped (were 8/4/4) a library-wide job (e.g. Duplicate Detection on 2026-06-01) fans the ML/thumbnail backfill out into a read storm that saturates sdc and starves etcd → apiserver down. `sidecar`/`smartSearch`/`faceDetection` stay at Immich defaults (small `.xmp` / SSD previews). Apply via Job Settings UI or the `system-config` API; **direct DB edits need a recreate of ALL immich pods to reload** (`immich-api` ×3 + `immich-worker` since the worker split; config cached at boot — UI/API edits instead fan out live to every replica over the socket.io Redis adapter, so prefer those). See `docs/post-mortems/2026-05-25-immich-anca-elements-io-storm.md`. |
| claude-memory | **A GPU tenant since 2026-09-01** (`stacks/claude-memory`): dense recall embeds every query, and on a CPU that cost 0.25-0.9s per call, putting recall p50 at 0.95s, p90 at 4.98s and pushing **6.5% of recalls past the session hook's 6s deadline** — those returned no memories at all. Now `Qwen/Qwen3-Embedding-0.6B` as an fp32 ONNX graph baked into the image, served by `onnxruntime-gpu` on the T4: **21-27ms per embed, ~0.10-0.17s end-to-end recall**. `gpumem` **5000** — arrived at by measurement, not estimate: a single model load reads 3,260 MiB but onnxruntime's CUDA arena GROWS under sustained use (4,236 MiB during a 14/s backfill, which tripped the watchdog's over-budget log at a 4000 declaration). Same arena-growth mechanism as the immich-ml row above, so **size a VRAM declaration from sustained load, never from one load**. Container memory limit is **4Gi, the tier-4-aux LimitRange CEILING** — 6Gi is rejected at admission (`maximum memory usage per Container is 4Gi`) and takes the Deployment to zero pods, which `terraform validate` cannot catch. **ACCEPTED RELIABILITY RISK (Viktor, 2026-09-01):** requesting `nvidia.com/gpu` hard-pins this to k8s-node1, so a node1 reboot or rebuild takes memory recall down for the pod restart (the ~5.5GB image makes a cold pull ~6 min). It was schedulable on any of six nodes before. A second replica does not fit the VRAM budget and the GPU request cannot be made optional; the risk is taken knowingly, not unexamined. Strategy is **Recreate** — the pre-existing self anti-affinity plus one schedulable node deadlocks a RollingUpdate. Rollback for anything dense is `MEMORY_EMBEDDINGS_ENABLED=0`. Alerts: six rules in the `Memory recall` group (p90, CPU-fallback, errors, pending embeddings, embed-write failures, telemetry absent). Plan: `claude-memory-mcp/docs/plans/2026-08-31-gpu-query-embeddings.md`. |
| CrowdSec | Pin version, disable Metabase when not needed (CPU hog), LAPI scaled to 3, **DB on PostgreSQL** (migrated from MySQL), flush config: max_items=10000/max_age=7d/agents_autodelete=30d, DECISION_DURATION=168h in blocklist CronJob. **Agent LAPI registration is made reboot-idempotent by Kyverno mutation `crowdsec-agent-idempotent-register`** (`stacks/kyverno/modules/kyverno/crowdsec-agent-register.tf`): the upstream chart (≤0.24.0) re-runs `cscli lapi register` on every init and LAPI 403s "user already exist" after a node reboot, wedging the agent (2026-07-12 node4); the mutated init skips registration when `/tmp_config/local_api_credentials.yaml` already exists. Re-diff on chart bumps. Manual unwedge (unmutated pods): `kubectl exec deploy/crowdsec-lapi -- cscli machines delete <machine>` then delete the pod. **Enforcement is a Traefik entrypoint plugin for HTTP + nftables below it** (since 2026-08-18): the in-process `crowdsec` middleware on `websecure` covers all HTTP incl. proxied hosts (bouncer key `traefik`, `stacks/traefik/modules/traefik/crowdsec-bouncer-plugin/`), and `cs-firewall-bouncer` DaemonSet drops in-kernel via nftables on direct hosts and non-HTTP ports (bouncer key `firewall`, v0.0.34 binary fetched at runtime, hostNetwork+NET_ADMIN, `stacks/crowdsec/modules/crowdsec/firewall_bouncer.tf`). Both fail open. The `crowdsec-cf-sync`/Cloudflare-edge channel and the `kvsync` key are GONE — see the Networking bullet for the 72h Lists floor that retired them. **Two hub scenarios are overridden locally** (`http-probing`, `http-crawl-non_statics`): upstream groups by `evt.Parsed.target_fqdn`, which NO traefik parser path creates (verified with `cscli explain` in both CLF and JSON on 2026-09-01 — the JSON node sets `evt.META.target_fqdn`, a different map), so the key collapsed to `<ip>/` and every host shared one bucket per IP — they now group by `evt.Parsed.traefik_router_name`. **The access log is JSON since 2026-09-01 and CrowdSec needed no change** — `crowdsecurity/traefik-logs` ships a JSON node next to its CLF grok; parity confirmed line-for-line with `cscli explain`, `evt.Parsed.status` stringifies so the `'403'`/`'429'` string compares still match. Same check found `viktor/immich-asset-paths-whitelist` inert in BOTH formats (it reads `evt.Parsed.target_fqdn`); left alone deliberately, since fixing it would start suppressing detections. Each custom scenario needs BOTH a ConfigMap key AND an `extraVolumeMounts` subPath entry over the hub symlink's path; the key alone is inert, and a subPath mount only refreshes on pod restart. See `docs/architecture/security.md` |
| Frigate | GPU stall detection in liveness probe (inference speed check), high CPU |
| Authentik | 3 server replicas + 2-replica embedded outpost (PG-backed sessions), PgBouncer in front of PostgreSQL, strip auth headers before forwarding. **`authentik.*` Helm values are INERT** (existingSecret skips chart env rendering) — tune via `server.env`/`worker.env` in `modules/authentik/values.yaml`. Single-screen login (password embedded in identification stage); all first-party OIDC apps use implicit consent (2026-06-10). `/static` ingress carve-out serves assets with immutable Cache-Control; `/`+`/static` use a dedicated `authentik-rate-limit` (100/1000) so the cold-load chunk burst isn't 429'd into a blank screen. **Reliability (2026-06-28): the chart key is `deploymentStrategy`, NOT `strategy`** — the old `strategy:` key was inert, so live ran the chart default 25%/25% and dropped a server pod out of rotation on every roll; now `maxSurge:1/maxUnavailable:0`. Readiness `failureThreshold:8` (~80s, was 30s): the DB-coupled `/-/health/ready/` returns 503 on a PG/pgbouncer blip, and with too-tight tolerance all 3 server pods left the Service at once → Traefik 502/504 (the episodic blank-screen + 30s-hang). gunicorn `max_requests=10000`/jitter=1000 decorrelates worker recycles from DB blips. Redis is GONE since 2026.2 (sessions+cache+channels on PostgreSQL, no external-cache option) — a short PG transient is now survived, but a TOTAL CNPG outage still takes authentik down. **Custom overlay image (2026-06-28):** server+worker run `ghcr.io/viktorbarzin/authentik-server:2026.2.4-patch3` (built by `.github/workflows/build-authentik.yml` from `stacks/authentik/Dockerfile` + `patch-compat-sfe.py`) with TWO guarded patches: **#1 SLOW-1a** — narrows the identification-stage `select_subclasses()` query (~1.4s→~14ms; bare upstream call LEFT-JOINs every source subtype); **#2 old-browser blank login** — `patch-compat-sfe.py` (a) extends `compat_needs_sfe()` to serve authentik's built-in no-JS **SFE** login to old Safari/WebKit AND **any iOS browser** (Chrome/CriOS, Firefox/FxiOS — all share the system WebKit) on iOS≤16.3, and (b) **injects static social-login `<a>` links into the SFE shell** (`flow-sfe.html`) since the SFE can't render Identification-stage sources — required for password-less accounts (e.g. emo = Google-only). The modern flow SPA is ES2022 (needs Safari 16.4+) and renders BLANK on older WebKit; every iOS browser shares that WebKit, so it's not browser-choice (emo's iPadOS-15.8 iPad hit this). SFE = the *real* authentik login (password + MFA + reputation, no auth downgrade) — chosen over a Traefik basic-auth fallback which would have put a spoofable-UA single password in front of `vbarzin→wizard` passwordless-root. Social link = plain redirect to `/source/oauth/login/<slug>/` (works on any browser); slugs (google/github/facebook) are static — re-verify on source changes. **Keel un-enrolled** for the ns → image pinned in `global.image` (repo+tag), **upgraded manually**: bump the Dockerfile `FROM` + the values tag (+ re-verify both patches) together, GHA rebuilds, then apply. |
| Kyverno | failurePolicy=Ignore to prevent blocking cluster, pin chart version. **In a Kyverno `mutateExisting` rule, `match` selects the TRIGGER while `mutate.targets` selects WHAT GETS PATCHED — they are different sets, and an unselected `targets` ignores your `match` entirely.** Copying the broad `targets` list from one rule into a narrowly-matched new rule set `keel.sh/policy=never` on all 229 workloads instead of 37 and disabled Keel cluster-wide for ~10 min (2026-08-16). `targets` also cannot just be dropped: with `mutateExistingOnPolicyUpdate` set, the validating webhook rejects the policy (`rules[N].mutate.targets has to be specified`). Put the SAME selector on both — `keel-annotations.tf` keeps it in one `local` so they cannot drift. **Validate any policy edit with `kubectl apply --dry-run=server -f <policy>.yaml` before pushing** — the webhook catches this class in seconds, and a CI apply does not. **Every keel.sh/* mutation is an ANNOTATION, never a label (2026-08-17).** The kubernetes provider manages `metadata.labels` as a whole map, so a label Kyverno adds and Terraform does not declare plans as a removal on every stack that sets `labels` on the workload — the same two-owners-one-field fight the rule exists to end, moved onto labels. Extra ANNOTATIONS do not diff this way, which is why the `trigger`/`pollSchedule` pair this policy also adds has never appeared in a plan. The label version ran for one day and put **10 extra stacks** into the nightly drift report (`kubernetes_deployment.forgejo` planned `- "keel.sh/policy" = "never" -> null`); the exclude rule now selects on the annotation instead, verified against live state as 0 workloads holding a label-only opt-out. **Terraform manages `metadata.labels` even when the resource declares no `labels` block at all** — it then manages the map as empty, so a controller-added label still plans as a removal. `homepage/homepage-cache` was in the same drift report for exactly that, on the `tier` label the `sync-tier-label-from-namespace` policy stamps; the fix is to declare the label (as dbaas does). An earlier version of this note claimed uptime-kuma and redis "declare no labels map and never drifted" — wrong on both counts: they declare the label inside `modules/<name>/main.tf`, which a top-level `grep stacks/<stack>/*.tf` misses. **Grep the module subdirectories before concluding a stack does not declare something.** **`keel-annotations.tf` carries TWO rules**: `add-keel-annotations` enrolls every workload in a `keel.sh/enrolled=true` namespace, and `keel-never-when-another-owner` (2026-08-16) un-enrolls the ones that declare `app.kubernetes.io/managed-by` — see Monitoring → "Image-ownership conflicts" for why a blanket enrollment fights Terraform/Helm/operators forever. Note the namespace exclude list in that file has a LONG leading comment naming ~20 namespaces; the live array is only 8 (the 2026-05-17 expansion removed the rest) — read the array, not the prose. **All mutating ClusterPolicies MUST scope `operations = ["CREATE"]`** (resource-governance.tf) — mutating on UPDATE re-injects `spec.dnsConfig`/priority etc. onto later pod updates, which the apiserver rejects as forbidden pod-spec changes → pods stuck `Terminating` forever (2026-07-18 outage: a pod created while the webhook was down had `dnsConfig=null`, then the job-controller's finalizer-removal UPDATE was rejected). `inject-ndots` was the one rule missing it; fixed 2026-07-19. |
| MySQL Standalone | Raw `kubernetes_stateful_set_v1` pinned to `mysql:8.4.8` exactly (migrated from InnoDB Cluster 2026-04-16; **pinned to 8.4.8 on 2026-05-18** after Keel-driven `mysql:8.4` → 8.4.9 bump stalled the DD upgrade and required a full PVC-wipe + dump-restore — see `docs/runbooks/restore-mysql.md` and beads code-eme8/code-k40p). `skip-log-bin`, `innodb_flush_log_at_trx_commit=2`, `innodb_doublewrite=DETECT_ONLY` (was ON; set dynamically 2026-06-30, code-oflt — stops doublewrite page-content writes ≈ −0.86 MB/s/~half MySQL's page-flush writes, keeps torn-page DETECTION; recovery = restore-from-backup, OK with BBU+UPS+daily mysqldump), `innodb_buffer_pool_size=2Gi` (raised 1→2Gi + mem limit 4→6Gi, code-oflt 2026-06-30 — pod was near-OOM at 3.7/4Gi; needs a restart), **`innodb_flush_neighbors=0`** (was 1; set dynamically 2026-08-16 — MySQL's own default is 0, the 1 was a deliberate HDD-era choice. Neighbour flushing drags contiguous dirty pages along on every flush, betting sequential beats extra pages; that bet does not pay here because writes land in the PERC write-back cache at 0.50 ms — faster than a 7200 rpm spindle can physically do — and sdc sits under an LVM thin pool, so filesystem-contiguous is not platter-contiguous. Before: 3.88M page writes/day vs ~880k row writes = 4.4x amplification, 64.9 GB/day of which 62 GB was page flushing). ConfigMap `mysql-standalone-cnf`. PVC `data-mysql-standalone-0` (5Gi initial → 30Gi via autoresizer, `proxmox-lvm-encrypted`). Service `mysql.dbaas` unchanged. Anti-affinity excludes k8s-node1. Bitnami charts deprecated (Broadcom Aug 2025) — use official images. **Write-reduction (code-oflt 2026-06-30):** MySQL was the #1 sdc bandwidth writer; ~60% of its writes were nextcloud webcal calendar churn — subscriptions re-importing every cron (Formula-1 `refreshrate=PT0S`, Tripit `PT15M`) thrashing `oc_calendarobjects_props`. Throttled to `P1D`/`PT1H` via `UPDATE nextcloud.oc_calendarsubscriptions.refreshrate` (app-data, NOT IaC — can regress if a CalDAV client re-adds a subscription with an aggressive rate). **Reboot self-heal (2026-07-19): has a `startup_probe` (5-min budget, `failure_threshold=30`) so InnoDB crash recovery after an unclean power-loss stop isn't SIGKILLed by the ~60s liveness budget (exit 137 crashloop). Liveness only arms after the first successful ping.** |
| phpIPAM | IPAM — no active scanning. `pfsense-import` CronJob (hourly) pulls Kea leases + ARP via SSH. `dns-sync` CronJob (15min) bidirectional sync with Technitium. Kea DDNS on pfSense handles all 3 subnets. API app `claude` (ssl_token). |
| qBittorrent | Queue preferences are **reconciled every 5 min** by the `qbt-ratio-monitor` CronJob (`stacks/servarr/qbittorrent/main.tf`, `required_queue_preferences`) — changing them in the UI/API alone is reverted, edit the Terraform. **`dont_count_slow_torrents = True` (2026-08-08) is load-bearing**: without it a stalled torrent counts as active forever, and five 0-seed `mam-farming` torrents held all 5 `max_active_downloads` slots so every healthy download sat at `queuedDL`/0 MB/s indefinitely (only a manual force-start moved them). A torrent under 2 KiB/s for 60 s now stops counting, which also frees upload slots and so helps — not hurts — the "completed private-tracker torrents must stay announceable" constraint above it. The same script **reaps dead torrents** (2026-08-09): zero progress AND no seeders AND older than `STALLED_MAX_AGE` (3 days), deleted with files so the empty `/audiobooks` dirs go too. Deliberately narrow — a PARTIAL download is never reaped even when stalled ("How Countries Go Broke" sat at 21.7% for days then climbed past 48% when a seeder appeared); `num_complete <= 0` spares torrents that simply haven't announced. Count exported as `qbt_dead_torrents_reaped`. Diagnose a "downloads not starting" report via `/api/v2/torrents/trackers` → `msg` (a MAM account set to **seed only** returns no peers while ratio still looks healthy), not the `/downloads` list, which just looks slow. |
| Audiobookshelf | **Memory limit is 512Mi DELIBERATELY — do NOT cut it back on a right-sizing pass** (`stacks/ebooks/main.tf`, raised 2026-08-08; a `DO NOT LOWER` comment sits at the `resources{}` block). Idle is only ~75Mi, so krr/VPA-style recommendations sampled between imports will keep suggesting a cut. The real peak is a library scan that **imports** new books: at 256Mi that OOMKilled the pod (exit 137) while importing 9 audiobooks — it restarted cleanly but imported nothing, so downloaded files sat on disk invisible to the library. Scans that find nothing new never approach the peak, which is why the headroom looks reclaimable. Requests stay 64Mi (Burstable, tier 4-aux); only the ceiling moved. SYMPTOM: audiobooks download to `/audiobooks` fine but never appear, and the pod shows `restartCount>0` with `lastState.terminated.reason=OOMKilled`. |
| Book Search | Anna's Archive is the **discovery** layer only — its fetch route is dead (verified 2026-08-08): md5 pages load fine via FlareSolverr ("Challenge not detected"), but the free `/slow_download/` endpoint serves a challenge FlareSolverr cannot solve (500, times out at 150 s) and `/fast_download/` needs paid membership, so every Stacks attempt failed from 2026-05-01. Ebooks are now fetched from **libgen**, whose md5 is the same as AA's: `GET ads.php?md5=<md5>` → parse the single-use `get.php?…&key=…` link → `GET` it **with ads.php as the Referer** (load-bearing). A plain GET of ads.php only ever returns HTML — that is why the old "direct download" never worked. Ebooks can't be sniffed by magic bytes (a MOBI starts with the PalmDB name field, `BOOKMOBI` is at offset 60), so validation rejects HTML instead. **Duplicate checks run against the library each download lands in** — ebooks vs Calibre `metadata.db` (read-only URI; `/calibre-library` is mounted in the pod) plus the ingest folder, audiobooks vs Audiobookshelf plus **in-flight qBittorrent torrents** (ABS only knows what it has IMPORTED, so two copies queued back-to-back were invisible to each other and both landed). Deliberately NOT cross-media: an ebook is never blocked because the audiobook exists — Viktor holds Principles in both. |
| Actual Budget | **TWO independent syncs — never conflate them.** (1) BANK sync: GoCardless → server, driven by the `bank-sync-<user>` CronJob (`0 0 * * *`), syncs only open + on-budget accounts. (2) FILE sync: browser ↔ server CRDT — the "Syncing has been reset on this cloud file" error is this one; click **Revert**, never "upload this file to be the latest version". **A green bank-sync metric does NOT mean the import was correct.** The CronJob's success test is the HTTP status of the `/banksync` POST; Actual's own `{newTransactions, matchedTransactions}` counts are discarded three layers deep. From 2024-12 to 2026-08-16 the sync re-imported the same transactions every night (~93% of rows created per run were duplicates) with every alert green: **a user rule with a `set account` action moves the freshly-imported row out of the account being synced, and Actual's dedupe is scoped to that account** (`WHERE imported_id = ? AND account = ?`), so the moved copy is invisible to the next lookup and gets re-inserted for the whole 89-day GoCardless window (hard-coded in `@actual-app/core`, no config knob). Any `set account` rule recreates the loop — exclude a transaction with a category/payee marker + report filter instead. Now measured by `bank_sync_excess_imported_rows` → `BankSyncDuplicateImports`. **Do NOT send `budget-encryption-password`** unless the file is genuinely E2E-encrypted: actual-http-api branches on the header's mere presence and takes a full `downloadBudget()` + ~20 MB backup-zip path per request — measured 110,000 ms (timeout) with it vs 108 ms without on a 118 MB file. Neither budget file is encrypted (`encrypt_keyid` null); fire-planner has always called the API without it. `var.budget_encryption_password` is really the server LOGIN password (passed as `ACTUAL_SERVER_PASSWORD`). Accounts with `account_sync_source IS NULL` have no bank link, are skipped by the http-api, still return 200, and still record success — check that column before believing an account syncs. Debug by reading the http-api pod's decrypted `/data/<budget-dir>/db.sqlite` with `better-sqlite3` at `/usr/src/app/node_modules/` (NOT `/app/`). Both images are Keel-managed; keep the TF `tag`/`http_api_tag` seeds level with live — 26.6.0 is below the migration floor (files carry v26.7.0 migrations) and a recreate at that seed reproduces the "client too old" break. Runbook `docs/runbooks/actualbudget-bank-sync.md`; post-mortem `docs/post-mortems/2026-08-16-actualbudget-bank-sync-reimport-loop.md`. |
| Linkwarden | **Bookmarks + the extracted readable text live in Postgres; only the ARCHIVE ARTIFACTS are files.** The `Link` row stores a *path* (`archives/<collectionId>/<linkId>.pdf`, `.jpeg`, `_readability.json`) while the bytes go to `STORAGE_FOLDER`. Until 2026-08-15 the Deployment had **no volume at all**, so every archive lived on the container's ephemeral overlay and was discarded on each pod restart while the app kept re-generating them (its `Restarting main browser (30-minute rotation)` log line is that work). Now `STORAGE_FOLDER=/storage` with the `linkwarden-archives-encrypted` PVC mounted there (`proxmox-lvm-encrypted` — saved pages are personal browsing history), and the Deployment is `Recreate` (RWO). **No backup CronJob deliberately**: archives are derived data, regenerable from the URLs in Postgres, which the nightly per-db `pg_dump` already covers. `STORAGE_FOLDER` is resolved with `path.join()`, so an absolute value works; the upstream default is the RELATIVE `data` (i.e. `/data/data`, inside the image's own source tree). **DATA-LOSS HISTORY:** the `linkwarden` Postgres DB was found completely empty on 2026-08-15 (0 rows in all 18 tables). Bisecting the NFS dump archive showed `pre_iscsi_migration.sql` (3 Mar 2026) still held the data while `pre-encryption-migration.sql` (13 Apr) did not, so the loss falls between those dates — most likely the ~11-14 Apr storage migration. 71 links / 15 collections / 1 user were restored from the 3 Mar dump; **keep that file** — the rolling 14-day dumps had all aged into schema-only copies. |
| Proxy / cluster VPN egress | `stacks/proxy` runs TWO products on ONE NordVPN tunnel: the per-user remote browsers (over WireGuard) and **cluster VPN egress** for any workload (gluetun's userspace HTTP `:8888` + SOCKS5 `:1080` listeners, which re-originate the request out `tun0` — local origination, not packet forwarding, so a consumer needs no privileges, sidecar or netns sharing). **Consumer contract is one env var**: `HTTPS_PROXY`/`HTTP_PROXY` (or `ALL_PROXY`, which httpx maps to a single `all://` mount covering both schemes) = `http://proxy-egress-uk.proxy.svc.cluster.local:8888`, or `socks5h://…:1080` — the `h` is load-bearing, plain `socks5://` resolves via CoreDNS and both leaks the lookup and can return home-geo answers. Always set `NO_PROXY=.svc.cluster.local,.cluster.local,localhost,127.0.0.1` plus any address-bound host (`myanonamouse.net` — its session is ASN/IP-locked). **Do not put CIDRs in `NO_PROXY`** — httpx splits on `/` and builds a malformed `all://10.0.0.0/8` pattern; suffixes match multi-label FQDNs but never bare short names. **Gateway index 1 is PERMANENT and Terraform-declared** (Deployment `proxy-gw-1` + Services `proxy-gw-1` UDP 51820 and `proxy-egress-uk` TCP 8888+1080), country United Kingdom: `PERMANENT_IDX`/`PERMANENT_COUNTRY` in `pool.py` — `alloc_subnet_idx` never hands out index 1, `plan_gateway` returns `reuse` for that country even while the pod is momentarily unlisted (rollout / `deletionTimestamp`), and `plan_reaping` + `delete_gateway` refuse it. **The broker must never recreate or reap it**: its Role grants no `apps` access so it cannot touch the Deployment, but it CAN delete the Service / peers ConfigMap / wg Secret and strip a live gateway of its routing. It occupies one of the existing `MAX_COUNTRIES - RESERVED_SLOTS = 6` slots (budget deliberately UNCHANGED), leaving five user-selectable. **`plan_gateway`'s `reuse` is not a liveness claim** — it cannot see the pod during a rollout, so `ensure_gateway` gates index 1 on a **Ready** pod and returns a retryable "not ready yet" instead of wiring a browser to a ClusterIP with no endpoint. **Two Secrets, two owners:** `nordvpn-wg` = the account-wide NordLynx key (broker, refreshed every `NORDVPN_KEY_REFRESH_SECONDS`, which is what gives the Reloader annotation something to fire on); `proxy-gw-1-wg` = this gateway's own WireGuard server key, created if absent by `ensure_permanent_gateway_secret()` at startup and every reaper tick and **never replaced** (the sidecar reads it once at container start). The broker Deployment carries a `checksum/broker-scripts` annotation so Terraform rolls and WAITS for new broker code before `depends_on` lets the gateway Deployment be created — a broker build predating `PERMANENT_IDX` reads the gateway pod's absent `proxy/last-used` as idle-since-epoch and would delete its Service/ConfigMap/Secret; the new broker also stamps that annotation once per pod as a second line of defence. **Gateway identity resolves from the `proxy/gw-idx` LABEL, never the pod name** — a Deployment's pods carry generated suffixes, so a `proxy-gw-1`-keyed pod GET/PATCH 404s silently (`k8s()` returns the status and no caller checks it). A gateway Service selector must carry `app=proxy-gateway` **alongside** `proxy/gw-idx`: browser pods carry `proxy/gw-idx` too, and selecting on it alone put a browser pod behind the gateway VIP for ~4 days while every "no endpoints" check read healthy. **`FIREWALL_INPUT_PORTS` (`51820,8888,1080`) is load-bearing and easy to miss** — gluetun's kill-switch drops inbound traffic to any port not listed, and a loopback test inside the pod does NOT catch it; only a cross-pod request does (`:6080` hit exactly this during the geo-browser build). Fail-closed comes from the kill-switch; readiness is `exec /gluetun-entrypoint healthcheck` (~30 ms, reads cached state) because the control server on `:8000` requires auth on every route in current images and the health server binds `127.0.0.1:9999` — **without a probe a pod with a dead tunnel still reads `Ready`**. No liveness probe on the same check (gluetun self-restarts the VPN; a kill mid-recovery risks NordVPN's ~10-min over-limit cooldown, memory #10182). One alert: `VPNEgressGatewayDown` (deployment has no available replica for 10m, warning) inline in `prometheus_chart_values.tpl`. Access is **open cluster-wide by decision** — no NetworkPolicy, no proxy credentials (`HTTPPROXY_USER`/`SOCKS5_USER` deliberately unset); NetworkPolicy is the first hardening step if tenancy assumptions change. **This Deployment is Keel-opted-out since 2026-08-16** — `keel.sh/policy=never` as an annotation in `egress.tf` (the matching label was dropped 2026-08-17; it declares a `labels` map, so the label was drift). It carries no `app.kubernetes.io/managed-by` label, so the `keel-never-when-another-owner` Kyverno rule cannot cover it; the digest pin alone left the wgserver sidecar's concrete version tag bumpable, and any image change here replaces the pod and drops the tunnel. **gluetun is ALSO DIGEST-pinned** to `sha256:e3272b29a4bc…` (the `:latest` build at commit `7eed6ea`, 2026-08-07), and the wgserver sidecar to `linuxserver/wireguard:1.0.20260223`. Pinning was originally out of scope but became mandatory: the Deployment is Keel-enrolled (`policy=patch`, hourly poll), so with a floating tag Keel re-pinned the live image and Terraform reverted it on every apply — the two fought and REPLACED THE GATEWAY POD six times in ~30 min on 2026-08-16, each round trip dropping the tunnel (bad here: NordVPN refuses an over-limit reconnect with a ~10-min cooldown, #10182). **Do NOT 'fix' that with `ignore_changes` on `container[N].image`** — that path is POSITIONAL and the live container order is `[wgserver, gluetun]` while the HCL declares `[gluetun, wgserver]`, so it CROSSES the images and takes egress down (done and undone, same day). **Do NOT bump gluetun to a release tag either**: SOCKS5 is UNRELEASED — the newest release v3.41.3 (2026-07-30) has no socks5 listener, so pinning to it silently leaves Service port 1080 with nothing behind it. Re-verify `socks5` appears in the gluetun startup log before trusting `:1080` after any bump. `SOCKS5_LOG` does not exist (only `HTTPPROXY_LOG`), and gluetun errors on unknown env keys. Startup settings scroll out of `kubectl logs` fast on a restarting pod — read them from Loki (`homelab logs query '{namespace="proxy", container="gluetun"} \|= "settings:"'`). **Anti-bot efficacy of a datacenter exit is UNPROVEN**: the pilot (book-search → Anna's Archive) faces a DDoS-Guard JS challenge that scores on ASN reputation, and NordVPN exits sit in hosting ASNs, so the tunnel may make that target harder rather than easier — measure the 403 rate before and after, and treat a worsened result as a finding. Full consumer contract + the WireGuard-sidecar escape hatch: `stacks/proxy/README.md`; design `docs/plans/2026-08-16-cluster-vpn-egress-service-design.md`. |

## Monitoring & Alerting
- **Alert-on-change routing** (alert-noise-reduction 2026-06-12, `route` block in `prometheus_chart_values.tpl`): warning/info notify ONCE then stay quiet while firing (`repeat_interval: 8760h` ≈ off); criticals re-ping every 6h (was 1h); `send_resolved` on. Standing state is reviewed via the daily digest, not re-pings.
- **Daily alert digest**: CronJob `alert-digest` (monitoring ns, `alert_digest.tf` + `alert_digest.py`) posts the full current board grouped by severity + resolved-in-24h to `#alerts` at 08:00 Europe/London. Stock `python:3.12-alpine`, pure-stdlib (no pip/apk at runtime — avoids the status-page-pusher disk anti-pattern, id=559); reads Alertmanager v2 + Prometheus; reuses the Alertmanager Slack webhook via the `alert-digest` Secret. Safety net for alert-on-change.
- **Helm `pending-upgrade` auto-heal**: `helm-unstick` CronJob (monitoring ns, `helm_unstick.tf` + `helm_unstick.sh`, every 15m) clears Helm release records wedged in `pending-upgrade` — the recurring #6073 class where Woodpecker's cancel-on-new-push SIGKILLs `helm upgrade` mid-flight, blocking the release AND the later stacks in the same CI run. **Prevention**: `helm_release.prometheus` sets `wait = false` so helm no longer holds a ~15-min in-flight upgrade during the slow Recreate+WAL-replay roll (shrinks the SIGKILL window to ~2s). **Cure**: the CronJob deletes only a `pending-upgrade` secret that is >30m old (past any legit helm `--wait`) AND whose release still has a `deployed` fallback revision — non-disruptive (reverts the release pointer, live workload untouched, memory #6073). Scoped per-namespace (Roles, no cluster-wide secret access) to `{monitoring, crowdsec, nextcloud}`; `DRY_RUN` env safety-valve; a wedge with no deployed fallback is left for a human. **Detection**: cluster_healthcheck #18 (`helm list -a` flags `pending`). authentik/kyverno/metrics-server are deliberately out of scope (ancient one-offs, far more sensitive secrets) — clear a rare wedge there by hand. **Memory sizing matters and is not cosmetic (2026-08-08):** `kubectl get secret -l owner=helm` buffers every matched Secret in RAM before the jsonpath projection — the `monitoring` namespace alone holds 61 release records (~8.3 MiB gzipped) and kubectl peaks at ~**128 MiB** RSS. The original 96Mi limit memcg-OOM-killed kubectl on every run (~7-8 kills/run, ~610/day on whichever node the pod landed — visible only as `node_vmstat_oom_kill`, since cadvisor emitted no `container_oom_events_total` for it), and the script's `|| true` turned that into an empty result: **the job exited 0 and reported success while never inspecting a single release.** Limit is now 256Mi (2x measured peak) and the script **exits 1 when a namespace cannot be listed** instead of treating a failed query as "nothing wedged". If release revisions keep growing, re-measure rather than assuming the ceiling still fits.
- **Prometheus must not be Keel-managed (2026-08-16)**: `prometheus-server` carries `keel.sh/policy: never` as an ANNOTATION only (`server.deploymentAnnotations` in `prometheus_chart_values.tpl`) — that is what Keel reads and, since 2026-08-17, what the Kyverno exclude rule selects on. It briefly also carried a matching LABEL (`kubernetes_labels.prometheus_server_keel_optout`), removed 2026-08-17: see the Kyverno row for why a keel.sh/* label is drift. Terraform declares every image in this release, so a Keel bump is always reverted by the next apply and the two just take turns — Keel patch-bumped the `prometheus-backup` sidecar `docker.io/library/alpine:3.21` → `library/alpine:3.21.7` hourly (it also drops the registry prefix) and each flip replaced the pod: **11 ReplicaSets in 14h, deployment generation 242, and only 213 of an expected ~720 self-scrape samples in 24h** (the self-scrape job inherits `global.scrape_interval: 2m`, so 720 is the 24h ceiling — it was collecting under a third of the time). The visible symptom was alerting, not Prometheus: a restart clears in-memory alert state, so every `for:` timer restarts from zero — `DriftStacksMany` (`for: 30m`) re-fired and re-resolved all day (each Slack `[RESOLVED]` lands within ~2 min of a new ReplicaSet) and `DriftStackErrored` (`for: 2h`) could never stay up long enough to fire. **Any flapping alert with a long `for:` is worth checking against `kubectl get rs -n monitoring` before believing the alert's own subject.** Same fight `stacks/proxy` hit on its gluetun image the same day — see the Proxy row, which resolved it by digest-pinning instead.
- **Image-ownership conflicts (root-cause fix, 2026-08-16)**: two controllers can both be authoritative over `spec.template.spec.containers[*].image` — Keel versus a Terraform `helm_release`, a raw `kubernetes_deployment`, or an operator like the authentik server — and then they rewrite each other **forever**. The loop is **silent by construction**: Keel logs an ordinary `resource updated`, the other owner logs an ordinary reconcile, neither knows the other exists. Every instance was found via an unrelated downstream symptom, never directly: a flapping alert (`prometheus-server`, whose hourly restarts reset every alert `for:` timer), a dropped VPN tunnel (`proxy/proxy-gw-1`), a downgraded app (paperless-ngx to `:1.5.0`) — or **not at all**: `authentik/ak-outpost-public` sat at **generation 497** being downgraded 2026.2.6→2026.2.4 every ~4h with nobody aware. Measured 2026-08-16: **37 of 206** Keel-managed Deployments declared a second owner (28 `Helm`, 7 `terraform`, 2 `goauthentik.io`), and the same per-workload patch had been applied by hand **five separate times** (5 exporters, postiz, dbaas/mysql, proxy, prometheus). **TWO halves, both needed.** (a) PREVENT: Kyverno rule `keel-never-when-another-owner` (`keel-annotations.tf`) sets `keel.sh/policy=never` as an **annotation** on any workload whose `app.kubernetes.io/managed-by` is `Helm`/`terraform`/`goauthentik.io`. **Annotation only — never a label** (see the Kyverno row). It's a positive *set*, not an exclude, because Kyverno's add-only mutate **cannot remove** the `policy=patch` it already stamped (the `match-tag` removal needed a one-off kubectl sweep for exactly this) — so an exclude alone would leave all 37 still fighting. First-party apps are untouched: ghcr `:latest`/SHA images deployed by CI `kubectl set image` carry no `managed-by` label and keep auto-updating, so the 2026-05-17 enrollment expansion still stands. (b) DETECT: `image-flipflop-detect` CronJob (`image_flipflop.tf`, every 6h) flags a Deployment that produced ≥3 ReplicaSets in 24h **and put back an image set it had already replaced** — behaviour, not a label, so it also catches the residue `managed-by` undercounts (a raw `kubernetes_deployment` has no such label; only the `# KEEL_IGNORE_IMAGE` marker protects those). Alerts `ImageOwnershipConflict` + `ImageOwnershipDetectorStale`. **Distinguishing a fight from ordinary churn is the whole difficulty** — monotonic upgrade histories, repeated identical images, container reordering and single rollbacks must all stay quiet; that logic is unit-tested (`python3 image_flipflop_detect_test.py`, 14 cases) and was verified against live state (1,980 ReplicaSets → exactly the 3 known offenders, no false positives). **A RECENCY GUARD keeps it honest after a fix**: a finding needs its last *round* — the most recent moment an already-seen image came back — inside `ACTIVE_WITHIN_HOURS` (6h), so a fight that has been fixed clears within one scan instead of lingering the whole 24h window. Keyed on the last round, deliberately NOT on the newest ReplicaSet, or an ordinary deploy after the fight ended would revive the stale finding. Trade-off: a fight whose rounds are more than 6h apart can need a second scan to surface. Memory limit 192Mi is **measured** (~18 MiB JSON, ~75 MiB peak RSS), not guessed — see the helm-unstick row for why that matters.
- **Do we have stray pods? (2026-09-01)**: `stray-workload-detect` CronJob (monitoring ns, `stray_workload.tf`, daily 07:05) subtracts what Terraform DECLARES from what is actually RUNNING. Kyverno cannot answer this — it decides whether a pod is *allowed* (trusted registry, not privileged, no host namespaces, no CAP_SYS_ADMIN) and never whether anything *declares* it, so an object admitted correctly months ago that later drops out of `.tf` keeps passing every rule; the flow trail is no help either, since an undeclared workload nobody talks to leaves no edge. **The declared set is readable from a CronJob, which the design assumed it was not**: Tier-1 state lives in the `terraform_state` database on CNPG (root terragrunt.hcl `backend = "pg"`, one schema per stack, table `states`), so an init container projects all 162 schemas with one `\gexec` psql session (`stray_workload_extract.sql` — 328 declarations, 1.3s, 8.9 MiB peak psql RSS, measured; the jsonb work stays server-side). A dedicated read-only role `tf_state_reader` is created by an idempotent bootstrap Job (`stray_workload_db_init.sql`) rather than reusing `pg-terraform-state`, whose credential can WRITE state — state-write is arbitrary infra takeover on the next apply, a much bigger blast radius than the other secrets in that namespace. **`ON_ERROR_STOP=1` is load-bearing**: a schema the reader cannot SELECT must abort the extraction, because a SHORT inventory reads as a cluster full of strays. The six Tier-0 stacks are the one gap (local SOPS-encrypted state) — their 11 workloads are committed as `declared_tier0.json`, regenerated by `scripts/gen-tier0-workload-inventory.py`; forgetting produces a visible false positive, never a false negative, and the alert says which script to run. **Five accounting rules, in order**: exempt.json entry with a reason → name match → membership of a declared `helm_release` (a chart's Deployments are not in state, only the release is — 50 objects) → `ownerReference` (apiserver-maintained, so preferred over the self-asserted `managed-by` label, and it is what keeps every CronJob's Job pods quiet) → `app.kubernetes.io/managed-by` naming a reconciler we run (only `goauthentik.io`, whose server builds its outposts with no ownerReference). Bare pods get a 24h grace window plus a label exemption for services that start pods on demand (`app=proxy-browser`). **Two refusal guards** (the helm-unstick lesson, applied up front): under 150 declarations, or findings above 25% of the live set → push `stray_workload_inventory_ok 0`, report nothing, exit non-zero. Alerts `StrayWorkloadDetected` / `StrayWorkloadInventoryBroken` / `StrayWorkloadDetectorStale`. **Verified against live state 2026-09-01: 339 declarations vs 840 live objects, 3 findings, all real** — `servarr/qbittorrent-exporter` (live since 2026-03-25, keel-updated, in no state file, no `.tf` and no commit: a resource deleted from source without a destroy; it carries this repo's own `tier` label, which is why an ownership-label approach would have missed it), plus two Succeeded pods left behind by hand (`immich/sw-30953`, `monitoring/helm-unstick-manual`). 32 unit tests: `python3 stray_workload_detect_test.py`. Memory limits are MEASURED (psql 8.9 MiB peak → 64Mi; python 37.2 MiB peak over a 3.61 MiB `PartialObjectMetadataList` payload → 128Mi) — see the helm-unstick row for why that is not cosmetic.
- **Drift detection re-plans before it reports (2026-08-16)**: the nightly run's first pass finds drift candidates against a frozen checkout; a second pass re-plans **only those stacks against current master**, and a stack that comes back clean is counted clean. The cron also moved from `@daily` (00:00) to **`0 4 * * *`**, off `renew-tls-certificate` (`@weekly`, Sunday 00:00). Without this, a certificate renewal landing 5 minutes into the run made **79 of 92 changed resources the same `module.tls_secret`** (126 stacks manage it) and the report read as a mass revert. Errored stacks are deliberately NOT re-planned. Full detail: `docs/architecture/ci-cd.md` → "The confirmation pass".
- **Cascade inhibitions** (`inhibit_rules`): `NodeDown` AND `NodeConditionBad`/`NodeDiskPressure` suppress downstream pod-churn alerts (PodCrashLooping/PodImagePullBackOff/PodsStuckContainerCreating/ScrapeTargetDown/*ReplicasMismatch); `T3ProbeLegDown` suppresses `T3ProbeDropBurst` for the same `leg`; plus existing NFS/Traefik/Authentik/Power/Tuya/iDRAC cascades. No `equal` on the node rules (pod alerts carry no `node` label → cluster-wide, like NodeDown). **Planned-maintenance mute (2026-07-12):** `NodeMaintenanceInProgress` (info; any node cordoned ≥1m — kured drain-reboots and manual cordons alike) inhibits the whole reboot-blast list (NodeDown/TraefikDown/replica mismatches/OOM/RecentNodeReboot/…) so a routine kured reboot produces ONE Slack line instead of ~19; `NodeCordonedTooLong` (warning, 2h, deliberately uninhibited) guards forgotten cordons. Inhibition is Alertmanager-only — kured's alert gate and the sentinel gate read PROMETHEUS, so the `RecentNodeReboot` 1h settle window and alert-blocking still work unchanged. `PodStuckPending` (warning, 20m, scheduled-but-not-starting — FailedMount/attach-wedge/image-pull class) alerts the n8n-style failure as itself.
- **ScrapeTargetDown scrapes only Ready endpoints** (relabel `keep __meta_kubernetes_endpoint_ready=true` on both `kubernetes-service-endpoints` jobs) — completed CronJob pods lingering as NotReady EndpointSlice addresses no longer fire phantom "down" alerts (tts/tripit/beads, id=4895). Replaces the old "exclude completed CronJob pods" guidance; a Ready pod with a broken metrics endpoint still fires.
- Alertmanager is now scraped (`extraScrapeConfigs` job `alertmanager`) → `alertmanager_notifications_total`/`_alerts`/`_notifications_failed_total` available; it had no `prometheus.io/scrape` annotation so notification volume was previously unmeasurable.
- Every new service gets Prometheus scrape config + Uptime Kuma monitor. External monitors auto-created for Cloudflare-proxied services by `external-monitor-sync` CronJob (10min, uptime-kuma ns). Mechanism: `ingress_factory` auto-adds `uptime.viktorbarzin.me/external-monitor=true` whenever the ingress is publicly served (`dns_type` `"proxied"`/`"non-proxied"` — for proxied that means riding the `*` wildcard since ADR-0021, not a per-name record; `"internal"` and `"none"` get none — set `external_monitor = false` explicitly on internal-only ingresses so the sync's default opt-in doesn't re-add a doomed monitor; see `modules/kubernetes/ingress_factory/main.tf`) — no manual action needed on new services. The `cloudflare_proxied_names` list in `config.tfvars` is EMPTY since the 2026-07-09 wildcard consolidation (alertmanager/grafana/prometheus ride the wildcard); don't check that list when debugging "is this monitored?" questions.
- **External monitoring**: `[External] <service>` monitors in Uptime Kuma test full external path (DNS → Cloudflare → Tunnel → Traefik). Config: `stacks/uptime-kuma/`; targets discovered from the K8s ingress API (the `cloudflare_proxied_names` legacy fallback is empty since the 2026-07-09 wildcard consolidation; proxied hostnames resolve via the `*` wildcard, ADR-0021). NOTE: the `ExternalAccessDivergence` alert is DORMANT since 2026-05-26 — its `external_internal_divergence_count` metric was emitted by the disabled status-page pusher; genuine external-vantage coverage is gatus on mx2 (ADR-0020) + `StatusPageDown` + the egress probes.
- **An absence-detection Loki alert needs `or vector(0)` (2026-08-28)**: a `< N` threshold over `count_over_time` goes SILENT at exactly zero, the case it exists to catch — `sum()` returns *no series* when nothing matches, so the comparison yields an empty result and never fires. Only `> N` rules are safe without it (empty correctly means "nothing happened"), which is why this had not bitten before: `TerminalUpgradesCollapsed` is the FIRST `<` rule in `loki.tf`, against 41 `>` ones. Wrap as `(sum(count_over_time(...)) or vector(0)) < N`; verified live by pointing the same expression at a filter matching no lines. The guard also makes such a rule cover its log stream ceasing to ship at all — usually what you want, since "the thing is dead" and "its telemetry is dead" both need a look. `TerminalUpgradesCollapsed` itself watches ttyd WebSocket upgrades (`{job="devvm-journal",unit="ttyd.service"} |= "WS   /ws"`, one line per SUCCESSFUL attach) over 24h, `< 50` against a measured 227-259/day baseline, `for: 2h`, warning. It exists because terminal mode did not connect from any touch device for three days (25-28 Aug) while the collapse sat plainly visible in that stream and nothing watched it; the 24h window is deliberate (person-driven traffic makes an hourly threshold false-fire nightly) and it catches DEAD, not degraded — 25 Aug's 57 would not have fired because desktop still worked. Runbook `docs/runbooks/terminal-lobby-upgrades.md` carries the two-branch triage: ttyd down = ordinary service problem; ttyd UP = the page is dying before it opens a socket, so read the frontend's own `TLDIAG` exception telemetry. **Those TLDIAG records are JSON behind a `... TLDIAG ` line prefix with an `event.name` field** — the `| logfmt | name = "..."` queries in terminal-lobby's ADR-0008 predate the shipped shape and return nothing; strip the prefix with `line_format` then `| json`.
- **sdc disk I/O (rebuilt 2026-08-16)**: the shared 2x12 TB spindle now has seven rules in the `R730 Host` group. Two SYMPTOM alerts (`warning`): `HDDReadLatencyHigh` (>50 ms per read for 15m, guarded on >20 read IOPS so an idle device can't trip it — reads are what the PERC write-back cache cannot absorb, so this is the closest thing to a user-visible latency SLI here) and `HDDSaturated` (util >85%). Three RATE alerts (`info`): `HDDHighReadRate` (>90 MB/s), `HDDHighWriteRate` (>20 MB/s — **was 10 MB/s for 20m, which sat between p95 and p99 and never fired once in 30 days**, through the whole period sdc was the documented bottleneck), `HDDHighIOPS` (>1600). Two DAILY-VOLUME alerts (`info`): `HDDDailyWriteVolume` (>600 GB/24h) and `HDDDailyReadVolume` (>700 GB/24h) — these exist because every rate alert uses a 5m window with a 15m `for` and is therefore blind to a workload that reads or writes steadily all day without ever spiking, which is exactly how sdc reached 464 GB written / 606 GB read per day with nothing alerting. **Every threshold is anchored to a measured 7-day distribution at or just above p99** (written into the rule comments — re-derive rather than copy if the workload changes). All carry `keep_firing_for` so one busy stretch is one episode, and an inhibit rule stops the five volume alerts notifying while `HDDSaturated`/`HDDReadLatencyHigh` already describe the same event. `SSDHighWriteRate` (sdb >2 MB/s) is deliberately a **wear** canary, not a performance one: the 850 EVO is at ~28.5% of its rated TBW and idle, and 2 MB/s sustained is 173 GB/day. **Thresholds need a revisit once a full week of post-2026-08-16 data exists** — the write/read baselines were cut that day and the daily ones were set against the OLD baseline with headroom.
- Key alerts: OOMKill, pod replica mismatch, 4xx/5xx error rates, UPS battery, CPU temp, SSD writes, NFS responsiveness, ClusterCannotTolerateNonGpuNodeLoss (N-1 memory-request headroom — this is the real headroom alert; there is NO `ClusterMemoryRequestsHigh`/`ContainerNearOOM`; **both halves of its expression filter `kube_pod_status_phase{phase=~"Running|Pending"}` — kube-state-metrics keeps publishing `kube_pod_container_resource_requests` for Succeeded/Failed pods until the pod object is GC'd, and counting those phantom reservations made it flap for days (2026-08-08: k8s-node3 read 28.29 GiB vs 18.33 GiB genuinely running, ~10 GiB of finished CronJob pods) and drove a real 4Gi→3Gi cut to prometheus on 2026-07-26 chasing an unreal problem. Any new capacity / request-sum alert needs the same phase filter**), ContainerOOMKilled, PodStuckPending, ImmichSmartSearchSlow (context-search latency / clip_index cache eviction), AuthentikRootRouter5xxHigh (all-3-server-pods-NotReady cascade → 502/503/504 on the authentik `/` router). **The Traefik scrape keeps `traefik_router_requests_total`** (per-router `code` label) — the drop-regex in the `traefik` scrape job never touched the request counter, so per-router 429/5xx is queryable + alertable. It also **keeps `traefik_service_request_duration_seconds_bucket`** (2026-08-15): `IngressTTFBHigh`/`Critical` are a **p95 over 30m**, not a mean over 5m. The mean was decided by a single request on services taking a few requests a minute — 81 firings in the 7 days to 2026-08-15, none of them a service that was actually slow (matrix: 2ms baseline, single spikes to 4.5s, 0.09 req/s, and criticals re-ping every 6h). Keeping just the service histogram costs ~1.3k series (~1.4% of head); the router/entrypoint buckets stay dropped, as those are the ~5k/pod cardinality driver. **A mean over a low-traffic service is not a latency signal — use the p95**, and note `protocol!="websocket"` is load-bearing (authentik's outpost websockets average ~126s, terminal's ~28s, entirely by design).
- **E2E email monitoring**: CronJob `email-roundtrip-monitor` (every 20 min) sends test email via Brevo HTTP API to `smoke-test@viktorbarzin.me` (catch-all → `spam@`), verifies IMAP delivery, deletes test email, pushes metrics to Pushgateway + Uptime Kuma. Alerts: `EmailRoundtripFailing` (60m), `EmailRoundtripStale` (60m), `EmailRoundtripNeverRun` (60m). Outbound relay: Brevo EU (`smtp-relay.brevo.com:587`, 300/day free — migrated from Mailgun). Inbound external traffic enters via pfSense HAProxy on `10.0.20.1:{25,465,587,993}`, which forwards to k8s `mailserver-proxy` NodePort (30125-30128) with `send-proxy-v2`. Mailserver pod runs alt PROXY-speaking listeners (2525/4465/5587/10993) alongside stock PROXY-free ones (25/465/587/993) for intra-cluster clients. Real client IPs recovered from PROXY v2 header despite kube-proxy SNAT (replaces pre-2026-04-19 MetalLB `10.0.20.202` ETP:Local scheme; see bd code-yiu + `docs/runbooks/mailserver-pfsense-haproxy.md`). Vault: `brevo_api_key` in `secret/viktor` (probe + relay).
- **Share-link analytics (Immich)**: Traefik access logs are **JSON since 2026-09-01** (`logs.access.format = "json"` in `stacks/traefik`) and keep `User-Agent`+`Referer`+`X-Authentik-Username`+`X-Auth-Fallback` (headers otherwise default-drop, so pre-2026-07-06 logs have `-`; `X-Auth-Fallback` stays empty until it is added to `authResponseHeaders`); Loki recording rules `immich:share_link_{opens,requests}:count1m` (per-slug) remote-write to Prometheus (26w — outlives Loki's 30d); daily `share-link-geo` CronJob (monitoring ns) pushes unique-visitor + per-country gauges (`immich_share_link_unique_ips{,_by_country}`, DB-IP Country Lite, internal + Meta-preview CIDRs excluded) to Pushgateway. Alert: `ShareLinkGeoStale`. Slug extraction is ANCHORED to the JSON key `"RequestPath":"` and requires 2xx/304 (read from `DownstreamStatus`) — UA/Referer are attacker-controlled; never extract metric labels from unanchored traefik-line regexes. The JSON anchor is stronger than the CLF byte position it replaced: Traefik escapes `"` as `\"` inside every value, so `"RequestPath":"` cannot appear inside a header value and `[^"]*` cannot cross out of one field. **`&` arrives HTML-escaped as `\u0026`** — a `?slug=` regex matching only a bare `&` silently returns zero, which is the trap in this port. Ported in the same commit: the two recording rules, `share_link_geo.py`, and `stacks/immich/download_truncation_detect.py`. Details: `docs/architecture/monitoring.md` → Share-link analytics row. **Loki chart GOTCHA (found 2026-07-06): ruler config key is `loki.rulerConfig` — `loki.ruler` is silently ignored**; it had been wrong since install, so NO Loki-ruler alert was delivered to Alertmanager until the rename (chart now pinned 7.0.0). Heavy ad-hoc Loki queries: use `kubectl port-forward svc/loki 3100` (the `.lan` ingress 504s at ~60s and pollutes the searched traefik stream with its own query URL).
- **Do Immich downloads actually finish? (2026-08-30)**: `immich-download-truncation` CronJob (immich ns, `stacks/immich/download_truncation.tf`, hourly at `:07`) joins Traefik access logs from Loki against `asset_exif.fileSizeInByte`. It exists because **a truncated download is logged as an ordinary success** — Traefik writes `200` + `Content-Length` before the body, so status code alone can never distinguish "delivered" from "connection died half way", and only the byte comparison can. **Two metrics, and conflating them is the trap:** `immich_original_downloads_cut` = a response carried fewer bytes than the file (ROUTINE on mobile — HTTP Range recovers it; in the session that prompted this, 5 of 9 assets were cut and 4 resumed byte-exact — **do not alert on this**, it fires for anyone on a train) versus `immich_original_downloads_unrecovered` = the range requests never covered the missing tail, so somebody holds a partial file. Only the second alerts (`ImmichDownloadsLeftPartial`, warning, `for: 30m`); it was exactly 1 in that session and named the genuinely broken photo. Detection logic + 13 tests in `download_truncation_detect{,_test}.py` (stdlib only, no runtime pip — the status-page-pusher anti-pattern, memory #559); run `python3 download_truncation_detect_test.py`. **A test caught a real bug worth keeping:** 206s with no preceding 200 (video seeking, or a resume whose 200 predates the window) must NOT read as truncation — the logic requires `best_200 > 0`. Metric labels are extracted from a POSITION-anchored regex on the request line, never a loose search, because User-Agent and Referer are attacker-controlled. **Outcomes are keyed on (asset, USER-AGENT), not asset alone (fixed 2026-08-31, commit 457c5e71):** the first version aggregated per asset, so one client's successful download cleared another client's partial — caught live when a diagnostic curl from the devvm cleared a file a phone had 11% of (replaying that window: asset-keyed reported unrecovered=1, client-keyed unrecovered=2). Traffic from 10/8, 127/8, 192.168/16 and 172.16/12 is dropped at PARSE time so our own tooling can never read as a viewer. The client key is the user-agent rather than the IP **on purpose**: a phone legitimately starts a download on one carrier address and resumes from another (observed moving between 185.139.138.x and 92.63.205.x mid-file), so IP-keying would report every mobile recovery as a failure. Residual limit, accepted: two people with byte-identical UA strings merge into one client, which under-reports rather than false-fires. Pushgateway job `immich-download-truncation` (that scrape job applies no metric allowlist, unlike `kubernetes-service-endpoints`). **TWO KNOWN LIMITATIONS remain unfixed (2026-08-31), both making it UNDER-report — a clean board is not proof nothing broke:** (a) `ranged` bytes accumulate across download ATTEMPTS, so an earlier successful resume masks a later partial (observed live: real cuts at 11:36 and 11:37 produced no finding); fix = reset `ranged` on a fresh 200, which also needs records sorted by timestamp since `query_loki()` asks for `direction=backward` and the fold is sequential. (b) Chrome's PARALLEL SEGMENTED downloads read as failures — it fetches a large asset as three concurrent requests (one 200 + two 206s, each ~a third, overlapping so parts sum past 100%) and the plain-200 leg is scored a `cut` every time; usable rule: 3+ requests for one asset within ~2s summing to >=100% is ONE successful download. Both are written up in the script's own docstring.
- **Authentik walling-off guard**: `blackbox-exporter` (monitoring ns, `stacks/monitoring/modules/monitoring/authentik_walloff_probe.tf`) probes each must-stay-public `auth = "none"` carve-out URL with `no_follow_redirects` and FAILS (`fail_if_header_matches` on `Location`) iff it 302s to Authentik. Catches a carve-out regressing (TF revert / deploy / `ingress_factory` `auth` default flipping back to `"required"`). Scrape job `blackbox-authentik-walloff` (1m) → alert `AuthentikWallingOffPublicPath` (`probe_failed_due_to_regex == 1`, for 10m, `lane=security` → posts to `#alerts` via the `slack-security` receiver, which keeps its `[SECURITY]` styling; the dedicated `#security` channel was abandoned 2026-06-25 — the shared webhook's app isn't a member of it). **To guard a new carve-out: add one line to `local.authentik_walloff_targets`** (a `service → URL` map; `valid_status_codes` includes 301/302 so legit redirects/404s stay green — only the Authentik `Location` fails the probe). `curl -sI '<url>'` must NOT show a Location to `authentik.viktorbarzin.me` before adding.
- **pfSense egress / WAN monitoring** (added 2026-06-28 after the 2026-06-27 egress-only incident — pfSense VMID 101 stopped passing internet egress for ~20 min while internal routing + Unbound stayed up, and NOTHING alerted: no egress probe existed and the cloudflared replica metric stayed green): `blackbox-exporter` gained `icmp_egress` + `dns_external` modules (+ `NET_RAW` on the pod) in `authentik_walloff_probe.tf`. Three in-cluster probe jobs (`wan-gateway-icmp` → 192.168.1.1, `internet-egress-icmp` → 9.9.9.9/1.1.1.1, `internet-egress-dns` → cloudflare.com via both) traverse the pod→node→pfSense-NAT path that fails. Alerts (group `Egress / pfSense` in `alerting_rules.yml`): `WANGatewayUnreachable`, `InternetEgressDown` (`max()==0` = both providers dead, not a single-provider blip), `ExternalDNSResolutionDown`, `EgressOnlyDivergence` (t3-probe `cloudflare` leg down WHILE `internal` leg up — the incident signature, reuses the existing t3-probe), `PfSenseVMDown` (`pve_up{id="qemu/101"}==0` while host up — does NOT catch a guest-internal reboot, `pve_up` tracks the qemu process). (The `Egress / pfSense` group, like ALL Prometheus alerts here, is **inline in `prometheus_chart_values.tpl`** — there is no `alerting_rules.yml` file; Loki-ruler alerts live separately in `loki.tf`.) Plus Loki ruler `CloudflaredTunnelConnLoss` (>20 edge-conn failures/5m; calibrated live: steady-state ~2/6h vs 37-85/5m in-incident; the cloudflared replica metric is blind to tunnel-connection loss). `WANGatewayUnreachable`/`InternetEgressDown` **inhibit** the downstream egress symptoms (ExternalDNSResolutionDown/EgressOnlyDivergence/CloudflaredTunnelConnLoss/Email*/ExternalAccessDivergence). Runbook: `docs/runbooks/pfsense-egress.md`. **Deferred (needs a live-pfSense change, not in this monitoring-only change):** point dpinger's monitor at the local gateway + widen thresholds, disable `gw_down_kill_states`, add a failover gateway group + auto-recovery watchdog, and ship pfSense system/gateway/routing syslog to Loki (today only filterlog → CrowdSec; those logs are NOT centrally queryable — id #6717). No Uptime-Kuma egress monitor was added (the `external-monitor-sync` is purpose-built for `*.viktorbarzin.me` Cloudflare-path discovery; the blackbox probes cover egress directly).

## Security Posture (Wave 1 — locked 2026-05-18)

Plan in `docs/architecture/security.md` + response playbook in `docs/runbooks/security-incident.md`. Beads epic: `code-8ywc`.

- **Identity allowlist for security rules**: ONLY `me@viktorbarzin.me`. NOT `viktor@viktorbarzin.me`, NOT `emo@viktorbarzin.me` (those don't exist). emo's identity scheme is unknown — ask before assuming.
- **Source-IP allowlist (K2, K9, V7, S1)**: `10.0.20.0/22`, `192.168.1.0/24` (Proxmox + Sofia LAN), K8s pod CIDR `10.10.0.0/16`, K8s service CIDR `10.96.0.0/12`, Headscale tailnet `100.64.0.0/10`, **+ `10.0.10.0/24` (VLAN 10, devvm) on K2/K9 only** (added 2026-07-06 — the devvm uses SA-token kubeconfigs against the apiserver, e.g. `chrome-service:emo-browser` kubectl port-forward; V7/S1 not yet, add if devvm Vault-OIDC / PVE-ssh becomes normal). **Policy: no public-IP access** — Vault, kube-apiserver, PVE sshd must transit LAN or Headscale. **K2/K9 LogQL gotcha (fixed 2026-07-06):** `sourceIPs` is a JSON *array* — no-arg `| json` never indexes it into `sourceIPs_0` (label stayed empty → matched *every* SA event, ~500-900/5min false-fire, surfaced only when the same-day `loki.rulerConfig` fix let the ruler deliver). Fix = explicit `| json sourceIPs_0="sourceIPs[0]"` + `sourceIPs_0!=""` guard; V7 unaffected (scalar `request_remote_address`). See `docs/architecture/security.md` → Allowlist. **One documented exception (2026-06-11): break-glass SSH** — PVE sshd on a WAN-exposed `:52222`, key-only, dedicated break-glass key only (`Match LocalPort`), rate-limited + fail2ban; intentionally cluster-independent so it survives an outage. As-built `docs/runbooks/breakglass-ssh.md`. (Replaced the 2026-05-30 port-knock design — circular Vault dep caused a lockout.)
- **Response model**: (I) Slack-only daily skim. All security alerts via Loki ruler → Alertmanager → the `slack-security` receiver, which posts to `#alerts` (it keeps its `[SECURITY/<sev>]` title styling so security-lane alerts stand out). Severity labels carried in the alert (critical/warning/info). No paging. The dedicated `#security` channel was abandoned 2026-06-25 — the shared `alertmanager_slack_api_url` webhook's Slack app isn't a member of it (a `#security` override 404s), so everything consolidated to `#alerts`.
- **Kyverno policies (wave 1)**: `deny-privileged-containers`, `deny-host-namespaces`, `restrict-sys-admin`, `require-trusted-registries` flip Audit→Enforce with the 31-namespace exclude list (memory id=1970). `failurePolicy: Ignore` preserved. Cosign `verify-images` deferred.
- **NetworkPolicy default-deny egress (wave 1)**: observe-then-enforce (γ approach) — Calico flow logs cluster-wide + GlobalNetworkPolicy log-only on tier 3+4, build empirical allowlist after 1 week, phased per-namespace enforce starting `recruiter-responder`. Tier 0/1/2 deferred. **The internal (ns-to-ns) half of each allowlist now derives faster from the east-west flow trail** (below): `SELECT DISTINCT dst_ns FROM edge WHERE src_ns='<ns>' AND action='allow'`. External egress is NOT in that table (empty-ns flows dropped) — those still come from the Calico flow-log W1.6 snapshot. Enforce-flips remain out of scope of the trail (observe-and-derive only; beads `code-8ywc`).
- **East-west flow trail (who-talks-to-whom, ADR-0014)**: Calico **Goldmane** (`goldmane.calico-system:7443`, gRPC/mTLS, ~60-min in-memory ring buffer — no etcd writes) + **Whisker** live UI (`whisker.viktorbarzin.me`, Authentik-gated) → **`goldmane-edge-aggregator`** streams Goldmane's `Flows.Stream` over mTLS and upserts the namespace-pair **edge set** (`edge(src_ns,dst_ns,action,first_seen,last_seen,flow_count)`, self-edges + public-internet flows dropped) into **CNPG DB `goldmane_edges`** → daily **`goldmane-edges-digest`** CronJob posts first-seen edges to `#alerts` (consolidated to `#alerts`; the `#security` channel was abandoned 2026-06-25 — the shared webhook's Slack app isn't a member of it, so a `#security` override 404s; see runbook). **CERT-REUSE GOTCHA**: the aggregator's mTLS client cert reuses the operator's Tigera-CA-signed `whisker-backend-key-pair` Secret (Goldmane verifies CA-chain only) — **re-apply `stacks/goldmane-edge-aggregator` if the operator rotates it** (symptom: no `last_seen` updates, `AggregatorDown`). Service identity = namespace, + `service-identity` label only in `monitoring`/`kube-system`/`dbaas`. Health: `AggregatorDown` + `DigestFailing` alerts + cluster-health #48. **WHISKER-WEDGE GOTCHA** (2026-06-28): the operator's `whisker` NetworkPolicy allows DNS egress only to kube-dns *pods*, but whisker-backend resolves goldmane via the kube-dns *ClusterIP* — Calico drops UDP DNS to a ClusterIP under a podSelector-only egress rule, so when whisker-backend's gRPC stream breaks and it re-resolves, it wedges and the UI goes **empty** (the aggregator, a separate pod, is unaffected). FIX = additive egress NP `whisker-allow-dns-clusterip` (`stacks/calico`, allows whisker→10.96.0.10/32:53); the `whisker-watchdog` CronJob is a backstop. Manual heal `kubectl -n calico-system delete pod -l k8s-app=whisker`. Runbook: `docs/runbooks/goldmane-flow-trail.md`. (Goldmane is OSS tech-preview — reversible operator-CR toggle in `stacks/calico/main.tf`.)
- **Who reached the kube-apiserver (design step 4, 2026-09-01)**: the apiserver already carries `--authentication-config=/etc/kubernetes/pki/auth-config.yaml` with the `kubernetes` + `k8s-dashboard` OIDC issuers, Terraform-owned in `stacks/rbac/modules/rbac/apiserver-oidc.tf` over SSH and **byte-identical to the node** (sha256 `bdefc260…97e16`, cross-checked against the apiserver's own `apiserver_authentication_config_controller_last_config_info{hash}` metric — that metric is the fastest way to confirm which config is live, and the file is hot-reloaded, so an issuer change needs NO restart and NO manifest edit). There are **no `--oidc-*` flags and none wanted** (legacy flags allow one issuer). Measured: **zero** OIDC-authenticated audit events in 24h, while the shared kubeadm admin cert (`kubernetes-admin`, `credential-id X509SHA256=7a03b1f4…0616`, expires 2027-04-17) produced 116 mutating events in 1h — every human, agent and cron job on the devvm shares it, so the audit log cannot tell them apart. Landed inert: a `kubernetes-agent` Authentik app (`stacks/rbac/authentik-kubernetes.tf`), a **third issuer prefixing username AND groups with `agent:`** behind `var.agent_oidc_enabled` (**default false — with it off the rendered config is byte-identical, so the null_resource trigger does not move and an apply cannot touch the control plane**; flipping it needs a LOCAL apply with `TF_VAR_ssh_private_key` + `-replace`, because CI has no SSH key), and group-keyed ClusterRoleBindings (`oidc-group-bindings.tf`) for `kubernetes-*` and `agent:kubernetes-*`. The prefix is load-bearing: the audit event records `user.username`/`user.groups` and NOT the issuer, so a second client on one issuer would stay indistinguishable, and the prefix also means an agent inherits no human binding (agents are bound read-only). Verify with `kubectl --context agent@homelab auth whoami`. Break-glass stays the shared cert (authenticates via `--client-ca-file`, so an Authentik outage cannot block it) — note `scripts/t3-provision-users.sh` reads that same kubeconfig as `ADMIN_KUBECONFIG`, so retiring it is a provisioning change too. Runbook `docs/runbooks/apiserver-oidc-agent-identity.md`.
- **NO Kubernetes read is audited anywhere, and TWO audit policies exist with only one live (2026-09-01)**: the apiserver reads `/etc/kubernetes/audit-policy.yaml` (hand-deployed from `scripts/k8s-apiserver-audit-policy.yaml`, whose rule 1 is `level: None` on `get,list,watch`), while `stacks/rbac/modules/rbac/audit-policy.tf` writes a DIFFERENT policy to `/etc/kubernetes/policies/audit-policy.yaml` that nothing reads — its rules WOULD have logged reads. The log path and rotation flags disagree too. **Left inert on purpose**: its `triggers` are content hashes, so changing them re-runs an SSH provisioner that rewrites the apiserver manifest and restarts the API on the single control-plane node, and CI applies that stack with no SSH key. Measured 6h: 74,060 audit events (update 40,353 / create 21,368 / delete 10,082 / patch 2,257), **zero reads**, against 366,219 read requests served. Auditing Secret reads would cost ~+147 MB/day (+36% audit volume, **+4.2%** of Loki's 3.54 GB/day) and lands on apiserver disk + Loki, **never etcd** (etcd is not even scraped). Also corrected: on-node audit history is **~2.8 days, not 30** — `maxsize=100`/`maxbackup=10` rotates every 6-7h at ~415 MB/day, so `maxage=30` never bites. Sizing + the narrower `get`-only variant: `docs/runbooks/apiserver-oidc-agent-identity.md` → "The audit read gap".
- **What's NOT in scope**: canary tokens (rejected — self-trigger risk with Viktor's normal `vault kv list secret/viktor` and `kubectl get secret -A` workflows), Falco/Tetragon (too noisy for Slack-only daily check), Cloudflare/GitHub audit polling (deferred to wave 2).

## Storage & Backup Architecture

### Storage Class Decision Rule (for new services)

Choose storage class based on workload type:

| Use **proxmox-lvm-encrypted** when | Use **proxmox-lvm** when | Use **NFS** (`nfs_volume` module) when |
|------------------------------------|--------------------------|----------------------------------------|
| **Any service storing sensitive data** | Non-sensitive app state (configs, caches) | Shared data across multiple pods (RWX) |
| Databases (user data, credentials) | Media indexes, search caches | Media libraries (music, ebooks, photos) |
| Auth/identity services | Monitoring data (Prometheus) | Backup destinations (cloud sync picks up from NFS) |
| Password managers, email, git repos | Tools with no user secrets | Large datasets (>10Gi) where snapshots matter |
| Health/financial data | | Data you want to browse/inspect from outside k8s |

**Default for sensitive data is proxmox-lvm-encrypted.** Use plain `proxmox-lvm` only for non-sensitive workloads. Use NFS when you need RWX, backup pipeline integration, or it's a large shared media library.

**NFS server:**
- **Proxmox host** (192.168.1.127): Sole NFS for all workloads. HDD at `/srv/nfs` (ext4 thin LV `pve/nfs-data`, 3 TB). SSD at `/srv/nfs-ssd` (ext4 LV `ssd/nfs-ssd-data`, 100GB). Exports use `async,insecure` options (`async` — safe with UPS + Vault Raft replication + databases on block storage; `insecure` — pfSense NATs source ports >1024 between VLANs).
- **Nextcloud as NFS browser**: Nextcloud (`nextcloud.viktorbarzin.me`) mounts the PVE NFS roots (`/srv/nfs`, `/srv/nfs-ssd`) inside the NC pod at `/mnt/pve-nfs` + `/mnt/pve-nfs-ssd`. Surfaced to users via two ACL patterns: (1) admin-only root browsers `PVE NFS Pool` + `PVE NFS-SSD Pool` (scoped to NC group `admin`); (2) per-archive mounts (e.g. `/anca-elements`) with `applicable_users` set to the owners. ACL is at the mount level via `occ files_external:applicable` — Files Access Control is NOT used (NC 30/31's workflow engine lacks FilePath / UserId checks). Manifest lives in `kubernetes_config_map_v1.nextcloud_external_storage_manifest` (`stacks/nextcloud/external_storage.tf`); a one-shot K8s Job applies it idempotently.
- **`nfs-truenas` StorageClass**: Historical name retained only because SC names are immutable on PVs (48 bound PVs reference it — renaming would require mass PV churn, not worth it). Now points to the Proxmox host (`nfs.csi.k8s.io` dynamic provisioning on `192.168.1.127:/srv/nfs`). TrueNAS (VM 9000, 10.0.10.15) operationally decommissioned 2026-04-13; VM still exists in stopped state on PVE pending user decision on deletion.

**Migration note**: CSI PV `volumeAttributes` are immutable — cannot update NFS server in place. New PV/PVC pairs required (convention: append `-host` to PV name).

**NFS CSI mount option requirements** (learned from [PM-2026-04-14]):
- **ALWAYS set `nfsvers=4`** in CSI mount options. NFSv3 is disabled on the PVE host (`vers3=n` in `/etc/nfs.conf`). Without this, mounts fail silently if kernel NFS client state is corrupt.
- **NEVER use `fsid=0`** in `/etc/exports` on `/srv/nfs`. `fsid=0` designates the NFSv4 pseudo-root, which breaks subdirectory path resolution for all CSI mounts. Only `fsid=1` (unique ID) is safe on `/srv/nfs-ssd`.
- **`/etc/exports` is git-managed** at `infra/scripts/pve-nfs-exports`. Deploy: `scp scripts/pve-nfs-exports root@192.168.1.127:/etc/exports && ssh root@192.168.1.127 exportfs -ra`
- **Critical services MUST NOT use NFS storage** — circular dependency risk. Alertmanager, Prometheus, and any monitoring that should alert about NFS must use `proxmox-lvm-encrypted`. Technitium DNS primary uses `proxmox-lvm-encrypted` (migrated 2026-04-14).
- **NFS PV template** (in `modules/kubernetes/nfs_volume/`): always include `mountOptions: ["nfsvers=4", "soft", "actimeo=5", "retrans=3", "timeo=30"]`

**proxmox-lvm PVC template** (Terraform):
```hcl
resource "kubernetes_persistent_volume_claim" "data_proxmox" {
  wait_until_bound = false
  metadata {
    name      = "<service>-data-proxmox"
    namespace = kubernetes_namespace.<ns>.metadata[0].name
    annotations = {
      "resize.topolvm.io/threshold"     = "10%"
      "resize.topolvm.io/increase"      = "100%"
      "resize.topolvm.io/storage_limit" = "5Gi"
    }
  }
  spec {
    access_modes       = ["ReadWriteOnce"]
    storage_class_name = "proxmox-lvm"
    resources {
      requests = { storage = "1Gi" }
    }
  }
  lifecycle {
    # pvc-autoresizer expands this PVC up to storage_limit; ignore drift on
    # requests.storage so the next TF apply doesn't try to shrink it back
    # (K8s rejects shrinks → apply fails). To bump the floor manually:
    # temporarily remove this block, apply the new size, re-add the block,
    # apply again.
    ignore_changes = [spec[0].resources[0].requests]
  }
}
```
- `wait_until_bound = false` is **required** (WaitForFirstConsumer binding)
- Deployment strategy **must be Recreate** (RWO volumes)
- Autoresizer annotations are **required** on all proxmox-lvm PVCs
- `lifecycle.ignore_changes` on `requests` is **required** to coexist with the autoresizer
- Every proxmox-lvm app **MUST** add a backup CronJob writing to NFS `/mnt/main/<app>-backup/`

**proxmox-lvm-encrypted PVC template** (Terraform) — use for all sensitive data:
```hcl
resource "kubernetes_persistent_volume_claim" "data_encrypted" {
  wait_until_bound = false
  metadata {
    name      = "<service>-data-encrypted"
    namespace = kubernetes_namespace.<ns>.metadata[0].name
    annotations = {
      "resize.topolvm.io/threshold"     = "10%"
      "resize.topolvm.io/increase"      = "100%"
      "resize.topolvm.io/storage_limit" = "5Gi"
    }
  }
  spec {
    access_modes       = ["ReadWriteOnce"]
    storage_class_name = "proxmox-lvm-encrypted"
    resources {
      requests = { storage = "1Gi" }
    }
  }
  lifecycle {
    # See data_proxmox above — required for autoresizer coexistence.
    ignore_changes = [spec[0].resources[0].requests]
  }
}
```
- Same rules as `proxmox-lvm` (wait_until_bound, Recreate strategy, autoresizer, backup CronJob, `lifecycle.ignore_changes`)
- Uses LUKS2 encryption with Argon2id key derivation via Proxmox CSI plugin
- Encryption passphrase stored in Vault KV (`secret/viktor/proxmox_csi_encryption_passphrase`), synced to K8s Secret `proxmox-csi-encryption` in `kube-system` via ExternalSecret
- Backup key at `/root/.luks-backup-key` on PVE host (chmod 600)
- CSI node plugin needs 1280Mi memory limit for LUKS operations (`node.plugin.resources` in Helm values)
- Convention: PVC names end in `-encrypted` (not `-proxmox`)

### 3-2-1 Backup Strategy
**Copy 1**: Live data on sdc thin pool (65 PVCs + VMs)
**Copy 2**: sda backup disk (`/mnt/backup`, 1.1TB ext4, VG `backup`)
**Copy 3**: Synology NAS offsite (two-tier: sda + NFS)

**PVE host scripts** (source: `infra/scripts/`; deployed manually via `scp` to `/usr/local/bin/<name>` — strip the `.sh`):
- `/usr/local/bin/nfs-mirror` — Daily 02:00. `rsync --delete /srv/nfs/<svc>/ → /mnt/backup/<svc>/` (sda leg 1), appends transferred paths to `/mnt/backup/.changed-files` for offsite Step 1. **EXCLUDES**: immich (too big — direct leg), frigate/temp (no backup), anca-elements (in Immich), and **(2026-06-01) ollama, prometheus-backup, audiblez, ebook2audiobook** — regenerable, live-only on sdc, kept off the space-constrained offsite. Does NOT mirror `/srv/nfs-ssd`.
- `/usr/local/bin/daily-backup` — Daily 05:00. Mounts LVM thin snapshots ro → rsyncs FILES to `/mnt/backup/pvc-data/<YYYY-WW>/<ns>/<pvc>/` with `--link-dest` versioning (4 weeks). Auto SQLite backup (magic number check, `?mode=ro`). Also backs up pfSense (config.xml + tar), PVE config. Prunes snapshots >7d. **Skip-list (2026-06-01)**: `nextcloud/nextcloud-data-proxmox` (orphaned pre-encryption PV).
- `/usr/local/bin/offsite-sync-backup` — Daily 06:00 (After=daily-backup). Step 1: sda → Synology `pve-backup/` (incremental via manifest; monthly full `rsync --delete` days 1–7). Step 2: NFS direct → Synology — **immich-only on BOTH `nfs/` and `nfs-ssd/` (2026-06-01)**; ollama/llamacpp on the SSD no longer ship offsite.
- `/usr/local/bin/lvm-pvc-snapshot` — Daily 03:00. Thin snapshots of all PVCs except dbaas+monitoring. 3-day retention (cut from 7 on 2026-06-29, code-oflt: fewer thin snapshots = less thin-pool tmeta CoW write-IOPS on sdc + ~1TB freed; `daily-backup` keeps 4wk file-level history). Instant restore: `lvm-pvc-snapshot restore <lv> <snap>`.
- `/usr/local/bin/vzdump-vms` — **WEEKLY, Sunday 01:00** (was daily until 2026-08-16). Live `vzdump --mode snapshot` of hand-managed VMs (the ones NOT in Terraform) → `/mnt/backup/vzdump/`, keep 3 per VMID (so 3 WEEKS of history now, not 3 days). **Went weekly because `.vma` has no incremental mode**: every run re-read devvm's entire 228 GiB disk in ~75 min = ~245 GB, about 40% of ALL reads on sdc, plus a 69.6 GB archive (~72% of sda's daily writes) — and during that window sdc had no headroom, which is where its ~90 ms p99 read latency came from. Day-to-day protection is now `devvm-home-backup` (below); this image is the **bare-metal restore floor** — it restores the whole VM in one shot, which rsync cannot. Recovering a destroyed devvm: `qmrestore` the newest image, then rsync the newest `devvm-home` generation over the top for the last day's work. `VZDUMP_VMIDS` default `102` (devvm) — **the only VM imaged today** (its per-user home dirs + local-only git repos, incl. the no-remote monorepo root, are otherwise irreplaceable). devvm has the guest agent (`agent: 1`) so dumps are fs-consistent. Deliberately NOT in the incremental offsite manifest (would balloon Synology); the monthly offsite full pass (days 1-7) mirrors `/mnt/backup/vzdump/`. Pushgateway job `vzdump-backup`. Added 2026-06-09 (closed the silent "VMs never imaged" DR gap). Restore: `qmrestore /mnt/backup/vzdump/vzdump-qemu-<vmid>-<ts>.vma.zst <vmid>`.
- `/usr/local/bin/devvm-home-backup` — Daily 03:30. File-level **incremental** backup of devvm's `/home` → `/mnt/backup/devvm-home/<YYYY-MM-DD>/`, `rsync --link-dest` hardlink generations, keep 14. Covers the same data `vzdump-vms` protects, but reads only deltas: the tracked set is ~33 GB (vs ~116 GiB raw `/home`) after excluding regenerable content (caches, `node_modules`, venvs, `.rustup`, `.vscode-server`, build dirs). Deliberately KEPT: `.ssh`, `.config`, `.claude`, `.gnupg`, `~/code` (git repos with uncommitted work; the monorepo root has no remote). **PULL, not push** — the PVE host holds the credential and reaches into devvm, so a compromised devvm cannot delete its own backups; the devvm-side key is pinned `command="/usr/bin/rrsync -ro /home",restrict` (read-only, `/home`-scoped, no shell). Key: `/root/.ssh/id_devvm_backup` on the PVE host. **devvm is 10.0.10.10, NOT 10.0.20.102** (the VMID is not in the address). Carried offsite by the monthly offsite-sync full pass, which uses `-H` so the hardlink farm doesn't explode into N full copies. Pushgateway job `devvm-home-backup`. Added 2026-08-16 — see the vzdump entry above for why.
- `nfs-change-tracker.service` — Continuous inotifywait on `/srv/nfs` + `/srv/nfs-ssd`. Logs changed file paths to `/mnt/backup/.nfs-changes.log`. Consumed by offsite-sync-backup for incremental rsync (completes in seconds instead of 30+ minutes).

**Synology layout** (`192.168.1.13:/volume1/Backup/Viki/`):
- `pve-backup/` — PVC file backups (`pvc-data/`), SQLite backups (`sqlite-backup/`), pfSense, PVE config (synced from sda)
- `nfs/` — mirrors `/srv/nfs` on Proxmox (inotify change-tracked rsync)
- `nfs-ssd/` — mirrors `/srv/nfs-ssd` on Proxmox (inotify change-tracked rsync)

**App-level CronJobs** (write to Proxmox host NFS, synced to Synology via inotify):
- MySQL (daily full + per-db), PostgreSQL (daily full + per-db), Vault (weekly), Vaultwarden (6h + integrity), Redis (weekly), etcd (weekly)
- **Per-database backups**: `postgresql-backup-per-db` (00:15, `pg_dump -Fc` → `/backup/per-db/<db>/`) and `mysql-backup-per-db` (00:45, `mysqldump` → `/backup/per-db/<db>/`). Enables single-database restore without affecting others.
- **Convention**: New proxmox-lvm apps MUST add a backup CronJob writing to `/mnt/main/<app>-backup/`

**Restore paths**:
- Single database: `pg_restore -d <db> --clean --if-exists` (PG) or `mysql <db> < dump.sql.gz` (MySQL) from per-db backup
- Accidental delete: `lvm-pvc-snapshot restore` (instant, 7 daily snapshots)
- Older data: Browse `/mnt/backup/pvc-data/<week>/<ns>/<pvc>/`, rsync back
- Database (full cluster): Restore from dump at `/srv/nfs/<db>-backup/` or Synology `nfs/<db>-backup/`
- pfsense: Upload config.xml via web UI, or extract tar for custom scripts
- Full disaster: Restore from Synology

## Known Issues
- **A Kubernetes upgrade erases the kubelet post-join tune on every node** (found 2026-08-16). `modules/create-template-vm/k8s-node-post-join-tune.sh` runs ONCE, as a cloud-init runcmd after `kubeadm join`, and nothing reconciles it afterwards. `kubeadm upgrade node` rewrites `/var/lib/kubelet/config.yaml` from the cluster-wide `kube-system/kubelet-config` ConfigMap, which does **not** carry these settings — so the v1.35.7 upgrade on **2026-07-26/27** wiped them from all six nodes (config mtimes 23:57→02:39, a rolling per-node sequence). Nothing alerted, and the first visible symptom arrived weeks later looking unrelated: the proxy VPN gateway could not start (`SysctlForbidden`, `net.ipv4.ip_forward` gone from `allowedUnsafeSysctls`), and a remote browser spent four days reconnecting to a Service with no endpoints. **Still missing on all six nodes as of 2026-08-16**: `shutdownGracePeriodByPodPriority` (this is the priority-ordered graceful-shutdown work — likely relevant to bead `code-xgcg`, "no working graceful shutdown on power loss"), `systemReserved`, `kubeReserved`, `evictionSoft`, `maxParallelImagePulls`, `memorySwap: LimitedSwap`. `evictionHard` and `containerLogMaxSize` survived. Only `allowedUnsafeSysctls` was restored (node2-5 only, the gateway nodes) — via `qm guest exec <vmid>` from the PVE host, editing `/var/lib/kubelet/config.yaml` then `systemctl restart kubelet`, one node at a time. **Detect with `scripts/check-node-kubelet-tune`** (reads each kubelet's live `/configz`, not the file on disk; exit 1 = drift, 2 = unreachable). Re-applying the rest changes memory reservations and eviction thresholds, so it can move pods — treat it as a deliberate change, not a cleanup. A durable fix means either putting these keys in the `kubelet-config` ConfigMap (which is what kubeadm writes node config from) or re-running the tune from the `k8s-version-upgrade` pipeline after each node upgrade; neither is built yet. Note `.claude/CLAUDE.md`'s "Node memory changes" bullet says the template is `stacks/infra/main.tf` — that stack does **not** manage these keys; the post-join script is the only source.
- **nvidia-namespace pods can't resolve cluster DNS** (found 2026-07-06): `getaddrinfo` fails for `kubernetes.default.svc` AND `*.svc.cluster.local` from *every* nvidia pod, despite a correct `/etc/resolv.conf` (nameserver `10.96.0.10`, ndots:2), a TCP-reachable kube-dns, and **no** NetworkPolicy in `nvidia` / no applicable Calico GNP (the only GNP `wave1-egress-observe-tier34` selects tier 3-edge/4-aux + is log-only; nvidia is tier `2-gpu`). UDP-53 to the kube-dns ClusterIP appears dropped while TCP:53 connects; **root cause not found**. Workaround: nvidia pods reach services **by IP** — `KUBERNETES_SERVICE_HOST` for the apiserver, a TF-interpolated Service ClusterIP otherwise (the gpu-vram-watchdog was migrated to this). Re-investigate if other nvidia-ns pods start needing DNS.
- **CrowdSec Helm upgrade times out**: `terragrunt apply` on platform stack causes CrowdSec Helm release to get stuck in `pending-upgrade`. Workaround: `helm rollback crowdsec <rev> -n crowdsec`. Root cause: likely ResourceQuota CPU at 302% preventing pods from passing readiness probes. Needs investigation. **Now auto-healed** by the `helm-unstick` CronJob (see Monitoring → "Helm `pending-upgrade` auto-heal"): a wedged crowdsec/monitoring/nextcloud release is cleared within ~15m (delete-pending-secret, non-disruptive) without manual rollback.
- **OpenClaw config is writable**: OpenClaw writes to `openclaw.json` at runtime (doctor --fix, plugin auto-enable). Never use subPath ConfigMap mounts for it — use an init container to copy into a writable volume. Needs 2Gi memory + `NODE_OPTIONS=--max-old-space-size=1536`. **`mcp.servers` baked into the ConfigMap-loaded openclaw.json gets stripped by `doctor --fix`** — register MCP servers via `openclaw mcp set <name> <json>` in the container startup command instead (CLI-written entries persist across doctor runs). Current servers wired this way: `ha`, `context7`, `playwright` (sidecar at `localhost:3000/mcp`).
- **OpenClaw memory-core indexes `/workspace/memory/`, not `/home/node/.openclaw/memory/`**: `/home/node/.openclaw/memory/main.sqlite` is the index store, NOT a content source. Files written under `/home/node/.openclaw/memory/projects/<x>/*.md` will NOT be indexed. To populate memory-core, write Markdown under `/workspace/memory/projects/<source>/` and run `openclaw memory index --force`. This is what the daily `memory-sync` CronJob in `stacks/openclaw/` does for claude-memory → OpenClaw sync.
- **(Obsolete 2026-06-12) Goldilocks VPA**: VPA/Goldilocks was uninstalled (etcd-load-reduction); the old "Goldilocks may have added a limit that blocks the change" gotcha no longer applies. Use `krr` for right-sizing.

## User Preferences
- **Calendar**: Nextcloud at `nextcloud.viktorbarzin.me`
- **Home Assistant**: ha-london (default), ha-sofia. "ha"/"HA" = ha-london
- **Frontend**: Svelte for all new web apps
- **Tools**: Docker containers only — never `brew install` locally
- **Pod monitoring / waiting**: Never use `sleep` — and `kubectl get pods -w` is a watch-and-guess, not a check. Wait on the CONDITION: `homelab deploy wait <ns>/<deploy>` for a rollout, `homelab ci watch [commit] [--repo <owner/name>]` for a pipeline, `homelab k8s rollout-status <app>` for a resource, or the `Monitor` tool with an until-loop for anything else. Measured over 175 sessions: 1,789 bare `sleep N` calls against 2 uses of `deploy wait`. Full rule + why a fixed sleep is not a check: execution.md §4.
